# Selected submit и серверный аудит: закрытый запуск 13 сентября 2026

В запуске **Алфея 27 → Домино 4 → Алфея 27** впервые совместно проверены offscreen direct, подача уже выбранных native Models и серверные возвраты `DrawInstance`. Сервер зарегистрировал **4 071 940 вызовов и столько же успешных возвратов renderer API**, без API errors и invalid mesh handles. Это подтверждает достижение renderer API; его SUCCESS после EmitCs не доказывает GPU completion, правильность каждого пикселя или полное прохождение уровней.

Запуск `play-rtx-20260913-070233-205`: игра PID 10380, сервер PID 9608, начало **07:02:35 МСК**. Оба процесса закрыты; сервер записал `queue_exit_normal`, watcher подтвердил восстановление bridge.conf **07:16:12 МСК**. Истина исходников — установленный `selected-v6`, а не последующие правки рабочего дерева, включая исследование lights. [Манифест](../../research/winx-remix-selected-submit-checkpoint-2026-09-13.json) содержит полные SHA, receipts, frozen входы, девять закрытых анализаторов и точные корреляции кадров.

| Установленный компонент | SHA256 |
| --- | --- |
| x86 adapter, установлен 07:02:21 | `1D6E585E66FC6A3481599307AB3D91ECC2B7336CBFFFA9449D868BB192D6D3F0` |
| x64 server с аудитом, установлен 06:45:43 | `D7FEE509F46B63F2202E563E30C519FA18D47373E044563213C7AAC4DB6437DC` |
| Прежний camera client | `79E88A694D233112605E7AB0F4F258F1FF536AD8471F67623C412DDAD6564F11` |
| Неизменённый штатный renderer | `F7C310821AA98BCDFDEC120330B0A89457B7C5EBA58D21464AF32639611C809F` |

Client frame и server presentIndex дают одинаковые **12 539 последовательных завершённых кадров 0..12538**, с единственным зарегистрированным device epoch на этих Present и без foreign Present. Замыкание серверных cumulative counters проверено по всем interval rows: **12 539 qualified complete**, три завершающих incomplete с нулём вызовов, cap не достигнут. Серверный аудит не содержит идентификатора клиентского способа подачи каждого instance; ниже сопоставлены итоговые счётчики одного завершённого кадра, а не асинхронные последние строки из live-снимков.

| Место / режим | Кадр | Direct | Selected | Прежний native | Renderer API SUCCESS | До первого оставшегося D3D draw |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Алфея, только offscreen | 2105 | 725 | 0 | 146 | 892 | 725 |
| Алфея, legacy | 2163 | 0 | 0 | 871 | 892 | 0 |
| Алфея, оба native | 2235 | 725 | 35 | 111 | 892 | 725 |
| Домино, только offscreen | 4559 | 158 | 0 | 288 | 450 | 0 |
| Домино, legacy | 4734 | 0 | 0 | 446 | 450 | 0 |
| Домино, оба native | 4857 | 158 | 282 | 6 | 450 | 0 |
| Алфея после возвращения | 12088 | 720 | 40 | 111 | 892 | 720 |

Сумма широкого native cohort в этих режимах сохраняется: **871 в Алфее, 446 в Домино**. Более узкий ordinary observer cohort: Алфея **725 + 35 + 6 = 766**, Домино **158 + 282 + 0 = 440**. Это количества подач, включая повторные occurrences; они не являются инвентарём уникальных объектов мира. После возвращения и изменения камеры распределение в Алфее менялось: кадр 12050 дал 677 direct + 83 selected + 111 прежних native, сохранив 871 и серверные 892.

Серверное `early` означает только «до первого оставшегося обычного D3D draw», включая первый draw с ошибкой/нулевым числом примитивов согласно контракту аудита. В Домино 158 независимых instances сосуществуют с early=0: первый оставшийся D3D draw мог относиться к UI/другому проходу, но его назначение этим аудитом не установлено. Нельзя переименовать этот счётчик в «до рендера мира». За весь запуск earlySuccess = **2 173 122**; часть приходится на фазу без native world camera.

Клиентские итоги: direct **2 645 216 instances / 2 396 888 groups**, selected **709 942 calls = 709 942 instances**, **104 036 precommit rejections** с прежним fallback. У обоих путей API failures и failure latch отсутствуют; selected reentries/postCommitFaults = 0, direct camera/queue rejection, unexpected draws и selection transitions = 0. Это не отменяет ограничений cohort. Клиентский cumulative material-channel counter включает selected, но исключает отдельный scopedDecal путь. В 42 сохранённых точках его сумма с direct не является полной разбивкой server total: остаток достигает **5946**. Замороженные roles.h/probe показывают отдельный overlay API вызов, sampled Домино содержит два overlays/кадр; полного независимого распределения остатка по каждому клиентскому вызову здесь нет. Остаток сохранён без подгонки.

Сравнение `alfea-native-modes-v1` — **5/5 camera/level matched**, state stack `[54,27,70]`: это сцена Алфеи с диалогом. Первая попытка `domino-native-modes-v1` остановилась после camera mismatch; baselineRestoredFile=true, неудачный результат сохранён. Повторная `v2` — **5/5**, state `[4]`; view/projection дополнительно совпали после упаковки captured float32. Автор отчёта просмотрел все пять изображений Домино: та же видимая ледяная геометрия сохраняется, частицы, idle-анимация Блум и UI меняются. Root просмотрел Алфею legacy/offscreen/both-native и первые/возвращённые кадры, а также Домино до/после движения. Возвращённая Алфея просмотрена также автором: окружение и персонажи видны, прежний оранжевый и пересвеченный вид остаётся. Pixel identity и исправление освещения не заявляются.

Оба F1-перехода завершились `loaded_and_presenting`: Домино **11,52 с / 42 кадра**, возвращение в Алфею **8,51 с / 38 кадров**. В Домино root дополнительно удерживал up 1,5 с; камера оказалась вплотную к склону. При следующем F1 зафиксирован active state **56**, затем корректно выбран 27. Это ограниченная проверка движения и загрузок, не full playthrough.

Особая граница: **5684..11982, 6299 кадров**, без свежего native main apply. На каждом таком кадре direct/selected=0, legacy native=2 и server API=2; sampled scene lights имеют scenes/owned=0. Это согласуется с наблюдавшейся фазой game over/UI, но точное назначение каждого кадра не восстановлено. Они не входят в покрытие мировой камеры и сами по себе не доказывают её дефект. Число быстро обновлявшихся UI-кадров нельзя переводить в длительность игрового движения. После возвращения main camera снова подана. В целом камера: **6028 submissions**, failures/stateMismatches/lateUpdates=0; missing windows указаны отдельно.

Оставшийся observer: **290 028 world/material/resource matches**, без различий этих данных и upload; **289 262 draw matches**. Сохранены **179 texture-token differences** в кадрах 167,168,176,2708,12006,12007 и bootstrap rejection 922 в кадре 176; в этих кадрах зарегистрированы texture writes. Это изменения currentness token, не сравнение texture pixels. Legacy native upload: **135 generations**, ноль upload/layout mismatches. Все анализируемые логи остались ниже cap; неполные наборы native frame rows отражают условия логирования, не произвольное заполнение пропусков нулями.

Readonly-проверка после выхода: user.conf, winx.ini, `.trex/bridge.conf` и четыре файла `Media/Saved` совпали по SHA с копиями до запуска. Автор ничего повторно не восстанавливал. EXE игры сохранил SHA из launch. В actual renderer logs остаются OMM memory-budget fallback и прежние **41 undisposed common device objects** при завершении; bridge32/64 сообщают успешную очистку. Sampled adapter resource failures/rejections = 0, pressure удалил 51 mesh и 3 материала; это не закрывает весь lifecycle.

Предварительные проверки сохранены отдельно: [selected CPU-контракт](winx-remix-selected-submit-contract-2026-09-13.md) **1544/1132 PASS** на едином source; общая HAL-регрессия **697 PASS** с selected=false. Этот игровой checkpoint добавляет фактические selected-подачи и серверные API-возвраты. Он не расширяет поддержку skins, particles, сложных материалов, физического освещения или произвольных lifetime-событий.