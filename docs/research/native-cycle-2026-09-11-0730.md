# Цикл исследования до 11 сентября 2026, 07:30 МСК

Начало: 10 сентября 20:22:23 МСК (17:22:23 UTC). Срок: 11 сентября 07:30 МСК.
Задача пользователя: все классы PC и PS2, приоритет PC; смежные исследования
объединять в общий проверочный пакет, когда это экономит время.
Разработка ядер и интерфейсов tools остаётся на паузе.

Начальная независимая оценка: PC **33,7817189632%**, PS2 **24,3906020558%**.
Оценены 733/733 PC и 681/681 PS2 зарегистрированных классов. Это экспертная
оценка поведения классов, не доля восстановленных инструкций. Полный охват
регистраций уже достигнут; новый прирост требует исследования рабочих методов.
Исходный commit `daf29ca`, рабочее дерево чистое. Snapshot базы:
`local-data/results/native-cycle-20260911-0730/baseline.json`.

## Организация

Пакеты выбираются по общей подготовке и взаимосвязям методов; сохраняются
отдельные PC/PS2 доказательства, границы исполнения и незакрытые ветви.
Сначала используется существующий указатель доказанных контекстов. Исторические
доказательства не редактируются, полные asset scans без причины не повторяются.
Первый пакет: рабочие виртуальные методы семейств AIAction, начиная с коротких
входов/сбросов и переходя к связанным алгоритмам и действительным зависимостям.

Результаты фиксируются локальными commits. Предыдущий запрет автоматической
проверки одобрения на публичную отправку GitHub остаётся неразрешённым; повторная
публикация без нового разрешения не выполняется.

## Проверенные блоки

### 1. AIAction: команды, вход и выбор состояния

21 класс, 36 paired command/input cases и 26 paired dispatch cases:
124 успешных исполнения PC/PS2. Общий command batch после одного пилота;
общий dispatch batch с четырьмя адресными повторными проверками. Время внутри
runner: 0,222+5,108+4,045+0,330 с; внешние Python startups не включены.
Полные state bodies не засчитаны: native dispatch останавливается на их входе.

Ошибка стенда: первое ожидание Troll state8/9 было переставлено; 4 первичных
отказа сохранены с точным source, таблица исправлена по настоящим PC/PS2 bytes,
повторены только 4 случая. После исправления все прошли. Неподтверждённый
borrowed gameplay-контекст при ошибочно продолженном PC handler не маскировался.

42 независимых assessment updates, без изменения знаменателя/весов:
PC **33,9522510232%** (+0,1705320600 п.п.),
PS2 **24,5741556535%** (+0,1835535977 п.п.). Это новые методы уже оценённых
классов; число зарегистрированных классов и полнота их закрытия не выросли.
27 уникальных evidence sources проверены, native FK и записанные строки совпали.
Подробности: [досье](native-ai-action-command-protocol-2026-09-11.md).

### 2. Рабочие переходы Golem, Spider и ShadowBeast

Пять путей состояний трёх классов: 51 paired case / 102 успешных исполнения.
PC v7 выбранных состояний возвращается целиком; PS2 исполняет dispatcher и
inline body после SQ-пролога до общей точки завершения. Подтверждены команды,
ожидание состояния персонажа, latch0/1/255, завершающие смены state и deadline.
Golem: uint32 now+RNG%301+200, включая wrap; Spider state1 расходует RNG,
хотя конечный deadline=0 (PS2 промежуточно вычисляет и затем обнуляет).

8 дополнительных успешных PC/PS2 проверок настоящего warm RNG output path,
без seed/twist. Первый PS2 zero-input исчерпал100ms до первой инструкции;
причина не установлена. Свежий guest500ms прошёл за0,021s; сохранены отказ
и обе точные версии runner. Остальные успешные RNG-входы не повторялись.
Handshake pilot1,125s + общий batch7,636s; внешние startups не включены.

6 assessment updates,25 уникальных источников проверены. PC **34,0068212824%**,
PS2 **24,6328928047%**. Незакрытые другие состояния и полноценный startup
не засчитаны. [Досье](native-ai-combat-handshakes-2026-09-11.md).

### 3. Общая зависимость: PS2 RNG seed/twist

Независимо исполнены default startup, next output, last warm element,
regeneration и next after regeneration:10 PC/PS2 transactions, все прошли.
Совпали output/index и все624 normalized state words, PS2 upper halves zero;
16-byte array guards сохранены. После первого результата задан отдельный
index623 boundary input; это не625-call serial replay. Пилот0,396s,
общий пакет продолжений0,685s, без внешних startups.

PC generator уже был исследован для частиц/FunctionEval и использован как
native эталон. Новый результат относится к полной PS2 подготовке и состоянию.
**Изменения оценок классов:0**; RNG не отдельная зарегистрированная сущность.
Support contract: `research/native-rng-transactions-2026-09-11.json`;
его источники отдельно проверены, нужно включить в итоговый аудит вместе
с native-platform manifests. Указатель контекстов дополнен новой версией
`research/native-context-index-2026-09-11.json`, старый сохранён.
[Досье](native-ps2-pc-rng-transactions-2026-09-11.md).

### 4. Изменённый payload Copy всего AIAction семейства

36 классов,31 distinct Copy entry на каждой платформе. 147 own transfers
независимо извлечены и сопоставлены PC/PS2;72 исполнения прошли. Два пилота
0,794s, общий пакет34 классов12,504s. Source=this,argument=destination;
проверены distinct finite word patterns/raw byteA5 и всё содержимое объектов.
PC factory+full Copy,PS2 own suffix after parent success либо base empty-prefix.
Опасный teardown намеренно изменённых pointer inputs не выполнялся и не засчитан.

Self-check подготовки выявил пропущенные PS2 LWC1/SWC1 в фильтре полей Baco;
raw evidence полное, исправлен только фильтр, его прежняя версия сохранена.
После исправления widths/order/offsets147 transfers совпали; native отказов нет.
Непустые destination graphs,self-copy и полная PS2 транзакция остаются открыты.

72 assessment updates,23 уникальных источника проверены. PC **34,2523874488%**,
PS2 **24,8972099853%**. [Досье](native-ai-action-modified-copy-2026-09-11.md).

Исследование продолжается.
