# Цикл исследования до 11 сентября 2026, 07:30 МСК

Начало: 10 сентября 20:22:23 МСК (17:22:23 UTC). Срок: 11 сентября 07:30 МСК.
Задача пользователя: все классы PC и PS2, приоритет PC; смежные исследования
объединять в общий проверочный пакет, когда это экономит время.
Разработка ядер и интерфейсов tools остаётся на паузе.

Начальная независимая оценка: PC **33,7817189632%**, PS2 **24,3906020558%**.
Оценены 733/733 PC и 681/681 PS2 зарегистрированных классов. Это экспертная
оценка поведения классов, не доля восстановленных инструкций. Полный охват
регистраций уже достигнут; новый прирост требует исследования рабочих методов.
Исходный commit `daf29ca`, рабочее дерево чистое. Snapshot базы:
`local-data/results/native-cycle-20260911-0730/baseline.json`.

## Организация

Пакеты выбираются по общей подготовке и взаимосвязям методов; сохраняются
отдельные PC/PS2 доказательства, границы исполнения и незакрытые ветви.
Сначала используется существующий указатель доказанных контекстов. Исторические
доказательства не редактируются, полные asset scans без причины не повторяются.
Первый пакет: рабочие виртуальные методы семейств AIAction, начиная с коротких
входов/сбросов и переходя к связанным алгоритмам и действительным зависимостям.

Результаты фиксируются локальными commits. Предыдущий запрет автоматической
проверки одобрения на публичную отправку GitHub остаётся неразрешённым; повторная
публикация без нового разрешения не выполняется.

## Проверенные блоки

### 1. AIAction: команды, вход и выбор состояния

21 класс, 36 paired command/input cases и 26 paired dispatch cases:
124 успешных исполнения PC/PS2. Общий command batch после одного пилота;
общий dispatch batch с четырьмя адресными повторными проверками. Время внутри
runner: 0,222+5,108+4,045+0,330 с; внешние Python startups не включены.
Полные state bodies не засчитаны: native dispatch останавливается на их входе.

Ошибка стенда: первое ожидание Troll state8/9 было переставлено; 4 первичных
отказа сохранены с точным source, таблица исправлена по настоящим PC/PS2 bytes,
повторены только 4 случая. После исправления все прошли. Неподтверждённый
borrowed gameplay-контекст при ошибочно продолженном PC handler не маскировался.

42 независимых assessment updates, без изменения знаменателя/весов:
PC **33,9522510232%** (+0,1705320600 п.п.),
PS2 **24,5741556535%** (+0,1835535977 п.п.). Это новые методы уже оценённых
классов; число зарегистрированных классов и полнота их закрытия не выросли.
27 уникальных evidence sources проверены, native FK и записанные строки совпали.
Подробности: [досье](native-ai-action-command-protocol-2026-09-11.md).

### 2. Рабочие переходы Golem, Spider и ShadowBeast

Пять путей состояний трёх классов: 51 paired case / 102 успешных исполнения.
PC v7 выбранных состояний возвращается целиком; PS2 исполняет dispatcher и
inline body после SQ-пролога до общей точки завершения. Подтверждены команды,
ожидание состояния персонажа, latch0/1/255, завершающие смены state и deadline.
Golem: uint32 now+RNG%301+200, включая wrap; Spider state1 расходует RNG,
хотя конечный deadline=0 (PS2 промежуточно вычисляет и затем обнуляет).

8 дополнительных успешных PC/PS2 проверок настоящего warm RNG output path,
без seed/twist. Первый PS2 zero-input исчерпал100ms до первой инструкции;
причина не установлена. Свежий guest500ms прошёл за0,021s; сохранены отказ
и обе точные версии runner. Остальные успешные RNG-входы не повторялись.
Handshake pilot1,125s + общий batch7,636s; внешние startups не включены.

6 assessment updates,25 уникальных источников проверены. PC **34,0068212824%**,
PS2 **24,6328928047%**. Незакрытые другие состояния и полноценный startup
не засчитаны. [Досье](native-ai-combat-handshakes-2026-09-11.md).

### 3. Общая зависимость: PS2 RNG seed/twist

Независимо исполнены default startup, next output, last warm element,
regeneration и next after regeneration:10 PC/PS2 transactions, все прошли.
Совпали output/index и все624 normalized state words, PS2 upper halves zero;
16-byte array guards сохранены. После первого результата задан отдельный
index623 boundary input; это не625-call serial replay. Пилот0,396s,
общий пакет продолжений0,685s, без внешних startups.

PC generator уже был исследован для частиц/FunctionEval и использован как
native эталон. Новый результат относится к полной PS2 подготовке и состоянию.
**Изменения оценок классов:0**; RNG не отдельная зарегистрированная сущность.
Support contract: `research/native-rng-transactions-2026-09-11.json`;
его источники отдельно проверены, нужно включить в итоговый аудит вместе
с native-platform manifests. Указатель контекстов дополнен новой версией
`research/native-context-index-2026-09-11.json`, старый сохранён.
[Досье](native-ps2-pc-rng-transactions-2026-09-11.md).

### 4. Изменённый payload Copy всего AIAction семейства

36 классов,31 distinct Copy entry на каждой платформе. 147 own transfers
независимо извлечены и сопоставлены PC/PS2;72 исполнения прошли. Два пилота
0,794s, общий пакет34 классов12,504s. Source=this,argument=destination;
проверены distinct finite word patterns/raw byteA5 и всё содержимое объектов.
PC factory+full Copy,PS2 own suffix after parent success либо base empty-prefix.
Опасный teardown намеренно изменённых pointer inputs не выполнялся и не засчитан.

Self-check подготовки выявил пропущенные PS2 LWC1/SWC1 в фильтре полей Baco;
raw evidence полное, исправлен только фильтр, его прежняя версия сохранена.
После исправления widths/order/offsets147 transfers совпали; native отказов нет.
Непустые destination graphs,self-copy и полная PS2 транзакция остаются открыты.

72 assessment updates,23 уникальных источника проверены. PC **34,2523874488%**,
PS2 **24,8972099853%**. [Досье](native-ai-action-modified-copy-2026-09-11.md).

### 5. CharacterState: разрешения и режимы переходов

57 классов,68 class/slot qualifications,42 PC и68 PS2 entry. 806 успешных
исполнений: по403 на платформу. PC400 full returns+3 consumer stops;
PS2367 full returns+36 bounded components. Время внутри runner66,305s.
Первый парный пилот, общий пакет8 частей (независимые части по2 процесса),
затем12 адресных проверок handles/сохраняемых слов. Native отказов нет.

Восстановлен контракт настоящего потребителя completion-like отметок:
PC4FB330/PS22A6C30,нулевой handle разрешён без мутации,совпавшие слова
потребляются. V13 может менять данные даже при итоговом запрете. V14 —
integer mode0/1/2. Проверены raw flag255,unsigned counter comparisons,
packed character bits,переключатели game/profile и записи move/profile.
MinotaurStunned target11 выполняет два Q: первый может потребить отметку,
после чего второй возвращает0. Это сохранено как поведение игры.

Открытые зависимые ветви: OpenGate — вход subscription message dispatcher
с27DE,12,0,0; MinotaurStunned — вход stop-action сargument0. Эти ветви не
засчитаны полными исполнениями. Lazy global startup остаётся вне borrowed
контекста. Подготовительный PS2 capture130 windows превысил лимит128;
пакет разделён100+30,исходный PC capture сохранён. Это не native отказ.

114 assessment updates. PC **34,7162346521%**, PS2 **25,3964757709%**.
16 direct sources/DB rows проверены importer verifier; ещё39 источников
внутри contract проверены отдельно. **Итоговому аудиту нужно обходить sources
новых contracts**, поскольку manifest ссылается на contract и относящиеся
к классу shards, не дублирует все39 ссылок114 раз.
[Досье](native-character-state-permissions-2026-09-11.md).

Пользователю уточнено расположение восстановленных исходников Sparkplug/Winx;
этот цикл пока добавляет исследования/контракты/пробы, а не новые C++ классы.
Изученность поведения не отождествляется с полнотой переноса в исходники.

### 6. CharacterState: управляющие поля и события анимации

19 классов,8 PC и19 PS2 методов. 244 исполнения,все прошли:122 полных PC
возврата,38 полных PS2 возвратов и84 PS2 post-SQ event components. Пилот0,257s,
общий пакет21,930s,без внешних Python startups. Кандидаты отобраны bounded
PC CFG walk без calls/indirect jumps;исключённые методы не получили credit.

Проверены сброс move word4,MinotaurStunned byte60,FrogJumping guard byte3C,
packed argument maskFFFFFFF2|2; события event_takeoff,air,event_air_begin/end.
Ghoul air записывает0/375.0/0 иbyte1;Troll/Yeti меняют разные байты3D/3C.
Имена совпадают целиком доNUL,с учётом регистра. Проверены префиксы/суффиксы,
другое имя,пустая строка,FF иNUL-tail. Все7 borrowed записей сравниваются целиком.

На PS2 настоящий strcmp40A8A8 исполнил byte/64bit пути приalignment1/8.
16-byte aligned LQ/MMI путь остаётся вне scalar стенда; пользователь уведомлён.
Ни этот путь,ни полные SQ/LQ event calls не засчитаны. Native отказов нет.

38 assessment updates. PC **34,8458390177%**, PS2 **25,5359765051%**.
Проверены6 direct sources/38 DB rows и31 transitive contract source.
Полностью закрытых классов и новых C++ реализаций не добавлено.
[Досье](native-character-state-data-events-2026-09-11.md).

### 7. wxAnimationManager: индексированный поиск и его предпосылки

20 paired inputs /40 различных PC/PS2 проверок. Exact key в исходной таблице;
только index0 при промахе использует alternate66;последний fallback — key0
в исходной таблице. Нулевой payload найденной записи не считается промахом.
Unsigned keys0/1/7FFFFFFF/80000000/FFFFFFFF,индексы0/1/66/67,точный порядок
настоящих lookup calls и сохранение4000h bytes borrowed storage проверены.
PC17 full returns+3 pre-read diagnostic stops;PS220 post-SQ components.

Отсутствующий mandatory default не имеет проверенного error return:
оригинал готовится прочитать end-node+10. PC stop59A64C,PS2 stop273D74.
Loader,обеспечивающий default,остаётся открытым. В diagnostic index67 PS2
вычисленный адрес manager468h лежит за exact allocation464h;настоящий read
после корректной остановки не выполнялся,ошибка нормальной игры не заявляется.

Три первоначальных PS2 diagnostic tests не прошли: stop был поставлен в
delay slot273D78,и Unicorn завершил load перед остановкой. Исходный runner
и отказы сохранены;после переноса stop перед branch повторены только3 случая,
все прошли0,059s. Остальные37 успешных входов не повторены ради этой правки.

Общий ps2_scalar_prefix теперь отклоняет фактически исполняемый stop в delay
slot. Проведены адресные negative/positive guard checks,они не увеличивают
число lookup cases. Сохранены старая,промежуточная и финальная версии wrapper;
промежуточная восстановлена из одной правки и совпала с записанным SHA.
253 прежние успешные PS2 stop-sites из40 result files текущего цикла
проверены статически;соседних transfer перед stop не обнаружено.
Первичная syntax typo runner исправлена до native startup,source сохранён.

2 assessment updates. PC **34,8594815825%**, PS2 **25,5506607930%**.
Проверены5 direct sources/2 DB rows и36 transitive contract sources+version
bindings. Иные методы manager/полный playback не закрыты,нового C++ кода нет.
[Досье](native-character-animation-selection-2026-09-11.md).

### 8. CharacterState: ключи анимаций и параметры запуска

16 классов,198 успешных исполнений:66 полных PC current-match calls,
66 PC changed→playback-entry stops,66 PS2 selector-entry stops. Пилот0,627s,
общий пакет22,655s,без внешних startups. Отказов нет.

Точные mask/bits для word0 аргумента независимо совпалиPC/PS2. PC использует
настоящий selector59A5E0 иdefault record,а не его замену. При совпадении
state24 метод завершается без запуска;при исходном0 проверены args настоящего
512EA0. IceWormAttack field8/10 выбирает flag0,остальные проверенные field —1.
Action/OpenGate при полном same-path обнуляют move4;MikaelOpenGate и
WandringNPCWait до changed playback ставят state3C=1. Полный nonzero-old→new
playback остаётся открытым. PS2 проверяет исходный packed-key builder после
SQ до273D10;полная вложенная транзакция не засчитана.

32 assessment updates. PC **34,9686221010%**, PS2 **25,6681350954%**.
Проверены5 direct sources/32 DB rows и23 contract sources. Новых C++ классов
или полностью закрытых классов нет.
[Досье](native-character-animation-requests-2026-09-11.md).

После исправления scalar wrapper адресно проверены прямые ссылки на live
research/ps2_scalar_prefix.py в текущих platform assessments и historical
native_research_evidence:0. Полный historical manifest/source audit остаётся
финальной проверкой цикла. Отчёт рядом с character-animation-selection.

### 9. wxAnimationController → spActor: команды и подготовка запуска

186 успешных исполнений: PC95 (59 полных),PS2 91 (49 полных). Остальные —
точные границы настоящих методов/события,без игровых success seams.
Пилот6 случаев0,825s;общие пакеты180 случаев18,946s суммарно внутри процессов,
первые два пакета параллельно. Отказов нет.

Доказаны собственные поля истории/request,Start/Stop/Fade dispatch и полная
цепочка finite fade. PC no-slot Start сохраняет новые поля и увеличивает
счётчик даже при отказе actor;FFFFFFFF→0. PS2 actor Find/Used/Fade исполняются
полностью,Start после SQ до настоящего event2 независимо совпал по выбору
слота,weight/restart,rates,priority и полному borrowed storage. Event queue,
binder,полный успешный игровой запуск и PS2 SQ/LQ остаются открытыми.

Три assessment updates: wxAnimationController PC20→35,PS2 15→30;
spActor PS2 20→35. PC spActor85 сохранён,его прежний credit не повторяется.
PC **34,9890859482%**,PS2 **25,7121879589%**. 33 contract sources закреплены.
Новых C++ или полностью закрытых классов нет.
[Досье](native-character-animation-playback-2026-09-11.md).

Исследование продолжается.
