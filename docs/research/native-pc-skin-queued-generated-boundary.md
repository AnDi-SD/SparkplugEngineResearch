# PC queued Skin first-generation: bounded failure record

2026-09-07, after CP85. No successful capture, no coverage credit.
The source CLI `--queued-generated` and probe/comparator are retained as
explicit research candidates, outside successful/default test profiles.

The CP85 whole queued draw is exact with a cached shader. Replacing the
prepared shader with the CP69 original EffectTemplate/Pass generation path
first exhausted contiguous476-byte template-pass placement: reserved64760,
free holes280/80/80/336. That call was not resumed.

New fixtures defer prepared camera input until after template creation and
retain a smaller explicit camera consumer view CC..233 (360 bytes, physical
base minusCC; the actual field reads are viewCC matrix and flag231). This
is an input view, not a Camera constructor claim. Actual RenderNode remains
fully constructed, owned and live. Full454850 reached actual generating
engine code but failed158-byte allocation at40F843→4123F0: reserved65248,
holes72/96/8/48/64. Alternative high placement and deferring the actual
support constructor made no improvement; no capped call was resumed.

All64KiB arena/32KiB request/100k instructions/2s original call/30s child
limits stayed unchanged. No unrelated object overlap, live input retirement,
allocation relocation, engine success stub, OS or GPU forwarding was used.
Continuation requires a defensible smaller input/lifetime arrangement or
separately proved allocation-layout strategy; repeating the same capped
configuration is not a next step.
