# Исследование всех классов PC/PS2 до 19:00 МСК 10 сентября

Начало: **10 сентября 2026, 08:06 МСК**. Граница: **19:00 МСК / 16:00 UTC**.
Цикл продолжается. Исходный Git checkpoint `a14d38c`.

## Объём

Все классы PC и PS2 EXE, с приоритетом PC. Игровая и движковая логика имеют
одну очередь. Разработка ядер tools на паузе. Правила оригинальной игры,
единой реализации, явных платформенных различий и спорных случаев сохранены.
Общий результат учитывается на обеих платформах при наличии их evidence.

## Исходный учёт

Read-only пересчёт реестра: последние оценки от 8 сентября 03:40:39 UTC.
Каталог объединения — 784 класса: 733 PC, 681 PS2, 630 есть на обеих платформах.

| Показатель | PC | PS2 |
|---|---:|---:|
| Весь каталог, class-weighted estimate | 17,48% | 11,37% |
| Классов с отдельной оценкой | 193/733 | 137/681 |
| Исторический SMO/SAN workflow | 45,62% | 31,16% |

Результаты 9–10 сентября сначала проходят адресную переоценку. Её прирост
будет отмечен как учёт уже выполненной работы, отдельно от новых открытий.
Не выдавать число оценённых классов за число полностью восстановленных.

## Очередь

1. Перенести подтверждённые результаты прошлых циклов в независимые PC/PS2
   assessments, без новых прогонов уже сохранённых экспериментов.
2. По существующему каталогу выбрать доступные семейства ранее неоценённых
   классов и общие базы; исследовать actual методы, а не только registration.
3. Сопоставлять PC и PS2 там, где это сокращает повторную работу. Не задерживать
   независимую очередь из-за одного protected метода или неподготовленного host API.

Ресурсы: около 1 ГиБ суммарной памяти; короткие directed проверки, bounded
процессы и сохранённые evidence. Локальные commits после проверенных блоков.
Большие исторические отчёты и успешные suites без причины не перечитывать
и не повторять. Новые результаты добавляются ниже компактными блоками.

## Результаты

1. [Учёт прошлых результатов](native-accounting-backfill-2026-09-10.md):
   внесено 37 assessments (31 PC, 6 PS2), из них 14/2 новых записей платформ.
   Общий PC показатель 17,48 → 18,88%, PS2 11,37 → 11,50%. Это accounting
   repair, не новые открытия. Знаменатели 733/681 сохранены; закрытых классов
   не добавлено. Preview на native-only таблицах, 29 evidence SHA и все
   37 applied rows проверены. Оставшийся старый учёт отмечен отдельно.
2. Кеш общей registration-инвентаризации обновлён один раз по pristine EXE:
   PC scan 2,679 с, PS2 0,598 с. Сохранены оба SHA и original registration
   records; сами registration не начисляют behavioral score. Эти данные
   используются для выбора семейства и сопоставления платформ.
3. [`wxGameFlowState`](native-class-wx-game-flow-state-2026-09-10.md): первая
   новая пара behavioral assessments. PC lifecycle/setter/reset/clone — 9
   оригинальных операций за 0,785 с, 4/4 owners освобождены. PS2 setter/getter
   и шесть base hooks — 11 случаев за 0,484 с. Обе версии формируют одинаковые
   56 STX имён; независимо установлены раскладки и различия initial codes /
   deleting ABI. Большие активные методы остаются открыты. Fresh PC lazy input
   startup остановился на отсутствующем renderer state; отказ сохранён,
   самостоятельный класс проверен с явно заданным borrowed external input.
   Новый прирост этого блока: PC +0,04775 п.п. до18,9318%, PS2 +0,04405 п.п.
   до11,5419%; это первые оценки класса35/30, а не его закрытие. Две записи
   предварительно проверены в native-only DB, затем applied rows/SHA сверены.
4. [Навигация HUD и resource remap](native-game-flow-navigation-remap-2026-09-10.md):
   PC29 original frames/1,245 с, PS215 direction prefixes/0,319 с. Найдены
   два реальных различия на границе угла из-за x87/float32 constants; условие
   обхода не является hard cap20, substring `empty` завершает переход.
   30 paired remaps и7 paired controller getters прошли за1,286 с. Первый
   PS2 remap остановился на неописанном stack restore; failure сохранён,
   граница следующего fresh probe перенесена до эпилога. GameFlowState теперь
   PC60/PS250; Controller10/10 только за один исследованный getter. Общий
   учёт18,9795% /11,5859%,209/733 и141/681 оценённых классов. Четыре записи
   прошли native-only preview и проверку applied scores/statuses/SHA.

5. [`spSubscriptionManager`: исправление по оригиналу](native-subscription-order-correction-2026-09-10.md):
   PC10 checkpoints/1,035 с, 51/51 выделений освобождено; независимо проверен
   PS2 unsigned pointer tree. Исправлены перепутанные PS2 Subscribe/Unsubscribe
   anchors и общий list-порядок callbacks. Новый regression сначала провалил
   прежнюю реализацию, затем прошёл на ordered set; SparkBaseTests1/1 за1,37 с.
   Mutation/reentry не закрыты. Ошибка MSVC include-prefix исправлена в локальном
   ignored CMake cache, затронутые translation units перекомпилированы.
   Manager теперь80/80; общий учёт19,0000% /11,5932%. Две applied rows и15
   evidence refs проверены. Два оставшихся выделения в Menu lifecycle отдельно
   атрибутированы живому пустому общему manager, не объявлены утечкой.

6. [34 потомка GameFlowState](native-game-flow-family-construction-2026-09-10.md):
   32 PC original lifecycle,160 вернувшихся операций; один OptionMenu factory
   blocked до возврата, abstract DialogueBox без PC factory. PS2 независимо:
   33 allocation literals,18 constructor bodies,34 vtable/getter identities.
   После4 pilot классов batch29 занял12,527 с при3 bounded процессах.
   Обе версии используют inherited empty copy; установлены различия размеров,
   собственные timers/input/subscriptions и untouched CentreScreen fields.
   66 первых assessments (PC32×20, PS234×15), без закрытых классов; учёт
   теперь19,8731% /12,3421%,241/733 и175/681 оценённых классов. Native-only
   preview,66 applied rows и38 уникальных evidence sources проверены.

7. [Короткие active hooks DebugMenu/DialogWindow/LoadSave](native-game-flow-small-hooks-2026-09-10.md):
   10 paired cases/1,772 с. Original PC zero600 bytes с guards, PS2 независимые
   memset args; порядок input virtual calls совпадает. У PC дополнительные
   DebugManager consumers, у PS2 дополнительные LoadSavePS2Manager действия:
   state+6A4==4 вызывает controller(54,1), четыре соседних/крайних состояния
   возвращаются без этого вызова. Игровые consumers ограничены явными входами,
   их body не заменяются заглушками. Шесть отдельных assessments повышены;
   общий учёт20,0573% /12,5330%. Native-only preview,6 applied rows и6 evidence
   sources проверены; число оценённых классов не изменилось.

8. [36 AIAction и отдельный PathFinder](native-ai-action-family-2026-09-10.md):
   все36 PC factory/clone возвращаются,35 полных class lifecycles,178 class
   operations. После одного pilot batch35 занял17,619 с при3 bounded workers.
   Подготовка настоящего пустого Node с borrowed core/scene устранила ненужный
   повторный bootstrap; исходные micro/1M caps и поздний missing-scene fault
   сохранены. PS2 независимо:36 sizes/vtables и14 ctor windows; все размеры
   на4 байта больше PC. Base notification code28/self-v9/forwarding, clear
   word и leaf hooks проверены попарно; PathFinder lifecycle отдельно0,948 с.
   Ошибочная early identity `wxPerception` исправлена по прямым vtable/getter
   обеих платформ; ошибочная метка PC leaf в стенде исправлена, первые11
   successful cases сохранены, оставшиеся5 проверены отдельно.
   Icy ctor/clone не заполняют два читаемых teardown указателя; PC отказ на
   allocator fillCC сохранён, PS2 corresponding reads/stores сверены. Normal
   init/allocator contract остаётся отдельной задачей, произвольных defaults нет.
   74 первых assessments, native-only preview,74 applied rows и55 evidence
   sources проверены. Общий учёт21,0805% /13,3700%,278/733 и212/681 классов.

9. [97 CharacterState классов](native-character-state-family-2026-09-10.md):
   97 original PC factory/RTTI/default clone,95 полных lifecycle,481 class
   operations; после root pilot batch96 занял32,531 с при3 workers.
   Только Book повторён с уже проверенным borrowed empty-scene контекстом.
   BeforeTrollFight и Goop teardown требуют непривязанный +18 collaborator;
   PS2 соответствующий безусловный вызов проверен независимо, normal binding
   остаётся открытым. Все97 PS2 factory sizes, vtable/getter identities и
   selector10 defaults совпали с PC; собственная инициализация12 фабрик
   отделена от общего вызванного конструктора. Все97 имеют empty copy slot.
   20 paired base hook/reset/leaf cases за2,782 с: nonzero handle выбирает
   consumer с0,4 либо прямой; consumers — граница, не подменённая реализация.
   Slots9/10 общие у всех97. Reset сохраняет владельцев и flags1F/20.
   194 первых assessments после native-only preview: общий учёт23,7340% /
   15,5286%,375/733 и309/681 оценённых классов. Закрытых классов не добавлено.
   Applied rows и111 уникальных evidence sources проверены за2,295 с.
   Избыточный общий FK-check базы остановлен; новый общий verifier проверяет
   только native tables и хеширует каждый источник один раз. Asset corpus
   не нужен для подтверждения импорта native assessments.

10. [36 CharacterStateMachine классов](native-character-machine-family-2026-09-10.md):
    PC35 factory/RTTI/default clone,34 полных lifetime,173 class operations;
    batch35 после root pilot17,111 с. Независимые PS236 constructor/vtable
    identities; поля вида машины совпали в35 сравнимых объектах. Размеры
    отличаются на12/16/20/24/28/40 байт, универсальный offset delta неверен.
    Goop machine требует+24 collaborator, обе версии вызова проверены.
    Bloom PC не вернулся при100k/1M/6M; дополнительная диагностика установила
    protected dependency wxCharacterMoveCtrl по exact vtable/getter. PS2
    Bloom initial state table отдельно содержит33 original allocations.
    71 первых assessments, native-only preview,71 applied rows/45 sources
    проверены. Общий учёт24,6821% /16,3216%,410/733 и345/681 оценённых классов.
    Base переходы захвачены для следующего этапа, credit за их поведение
    этот блок не начисляет; полностью закрытых классов не добавлено.

11. [Block tracing PC и завершённый Bloom lifecycle](native-pc-block-trace-2026-09-10.md):
    четыре fresh original сравнения дали одинаковые GPR/x87, хеши mapped
    PE/stack/arena и allocation/clone/timer observations. Внутри вызовов
    ускорение2,26–10,64×; Bloom6M15,610 →1,466 с. Это не ускорение всего
    исследования в10 раз. Прежние18 guard/cache tests и9 block tests прошли;
    после extended profile10 block tests также прошли. Guard учитывает REP
    prefixes; block cache сбрасывается при host/guest/self/cross-page writes.
    `protected-block` даёт60M при прежних24с/30с и памяти; instruction tracer
    его не принимает. Новый fresh Bloom run прошёл пять original операций
    за2,340 с, PC size716/PS2 static752. Семейство теперь36 PC factories и35
    полных lifetimes,178 class operations; Goop owner binding остаётся открыт.
    Новый PC Bloom score20, native-only preview и applied row/SHA проверены.
    Учёт24,7094% /16,3216%,411/733 и345/681 оценённых классов. Tool acceleration
    сама по себе не начислялась как изученность классов.
    Перед финализацией стенда исправлен [ложный отказ prefix внутри блока](native-pc-block-boundaries-2026-09-10.md):
    validation обрезается перед exact boundary/fixture, seams заморожены на
    время вызова. Negative regression сохранён,13 checks прошли за3,557 с;
    повторён только изменённый block original lifecycle, state SHA совпал
    с прежним instruction baseline. Исторические snapshots не перезаписаны.

12. [Общий протокол CharacterStateMachine](native-character-machine-protocol-2026-09-10.md):
    35 PC cases/33 paired PS2 cases;26/24 за4,431 с и9 filter cases за1,396 с.
    Проверены enter true/false,reset,code→mode,PC switch/push/pop и события
    0x2720/2722/2723,42 state pointers и два5-word стека на граничных valid
    depths. PS2 switch/pop достигли actual per-object notify100520 с теми же
    аргументами/словами,PS2 push пока static; остальные short leaves/prefixes
    сопоставлены явно. Notify28/30 выбирают разные virtual consumers;27F3
    сбрасывает машину,обычные сообщения передаются current state при nonnull.
    V11 читает flags связанного machine+24 объекта и проверяет mask18;null
    guard отсутствует. База PC20→45/PS215→35,остальные потомки не повышены.
    Native-only preview,две applied rows/11 sources проверены. Учёт24,7435% /
    16,3510%,число оценённых классов411/345 не изменилось. Normal binding,
    heavy tick и live observer mutation остаются открыты.

13. [wxEntity и прямые связи регистрации](native-entity-direct-family-2026-09-10.md):
    71 общая строка;67 callable factories,из них одна ранее оценённая машина.
    Новые66 PC factories/RTTI,64 clones,61 полный lifetime,318 class operations;
    PS267 independent static size/constructor/identity. Batch65 за30,570 с,
    seven cap follow-ups5,874 с,шесть empty-scene lifetimes12,218 с.
    Две clone остановки устранены подключением подтверждённого MSVCR71 strncpy;
    исправлена длина чтения в общей CRT fixture,5 regression cases прошли.
    Пять различий RTTI parent/реальной цепочки конструкторов подтверждены
    независимо PC/PS2:Projectile от spBaseObject,четыре класса через GenericTrigger.
    Наблюдение реальных vtable writes шести PC factories заняло1,535 с.
    У GenericTrigger подтверждён промежуточный ctor при нулевой factory.
    Остались clone MoveCtrl/Projectile и teardown AudioListener/Barrel/HUDPart;
    их разные зависимости не подменялись одинаковой причиной по адресу ошибки.
    134 новых platform assessments,native-only preview/applied rows/SHA verified.
    Учёт26,5102% /17,8194%,478/733 и412/681 оценённых классов. Полностью закрытых
    классов не добавлено;активная логика семейства остаётся отдельной работой.

14. [Entity reference/flags/copy/manager handoff](native-entity-reference-flags-2026-09-10.md):
    spEntity PC40-byte factory/RTTI/clone/teardowns0,424 с;PS2 независимая
    inline factory40-byte.10 paired reference-assignment cases3,127 с:
    uint16 retain/release,identity no-op,последняя ссылка и destructor entry.
    wxEntity24 PC cases3,722 с:15 cached/pause flags,6 distance cases,3 copy
    payloads. PS218 matching cached/payload prefixes и5 отдельных finite
    comparison tails;accumulator arithmetic не исполнялась. PC-only NaN
    результат не переносится на EE. Наблюдение двух original factory/teardown
    сценариев0,584 с подтвердило engine-add→engine-remove→game-add для wxEntity.
    Это исправляет прежнюю формулировку про регистрацию сразу в двух менеджерах.
    PC spEntityManager exact32-byte allocation и vtable6DC460 уточняют старое
    досье. wxEntityManager независимо имеет PC24056/PS215948 байт;normal binding
    entity+24 ещё не завершён. Общая PS2 prefix обвязка исключает SQ/LQ,MMI,
    COP2 и unreviewed COP1 accumulator;5 targeted tests0,103 с. Семь assessments
    previewed/applied/verified,36 уникальных sources проверены;учёт26,6126% /
    17,9148%,480/733 и414/681 оценённых классов. Ни одного нового closed класса.

15. [wxEntityManager tables/classifier](native-entity-manager-tables-2026-09-10.md):
    standalone PC factory/RTTI/clone/two teardowns0,704 с,all tracked allocations
    released,arena48224 байта. Original PC matrix40×130 противPS2 matrix40×80;
    zero-runs и unwritten слова проверены раздельно. Два PS2 prefixes завершили
    ordinary preparation/table loops,аппаратная запись82→10000010 не исполнена.
    20 classifier пар:сначала равенство entity/profile record,затем Button42,
    HUD41,поискslots1..39 и первый свободный индекс. Run1 прошёл17 пар и два
    ctor prefixes,затем остановился из-за неверного zero-extension input hash.
    Corrected original-LW register preparation и только три оставшихся fresh
    cases прошли за0,435 с. Manager PC15→30/PS215→25;две applied rows/24 sources
    verified. Учёт26,6330% /17,9295%,480/733 и414/681 классов. Normal binding и
    динамические capacity branches обработки выбранной группы ещё открыты.

16. [wxEntityManager insertion/removal](native-entity-manager-insertion-removal-2026-09-10.md):
    18 paired post-classifier prefixes2,603 с;пределы обычных групп130/80,
    HUD100/100,Button130/105 и MoveCtrl130/80. Полный capacity duplicate scan,
    новые slots1/39 и полные массивы проверены guard всего manager. Prefixes
    заканчиваются до original list append;диагностика Button не подменяется.
    13 paired complete array-removal helpers2,344 с:сдвиг до конца capacity,
    последнее слово0,count--,соседний дубликат после сдвига не перепроверяется.
    Guards охватывают inactive tail и счётчик. Исправлена одна прежняя подпись
    PS2 entry:register начинается2892C0,а2892B0 — return предыдущего helper.
    Manager PC30→45/PS225→40;две applied rows/30 sources verified. Учёт26,6535% /
    17,9515%,480/733 и414/681 классов. Полный manager callback/list lifecycle,
    producer entity+24 и полноценный PS2 startup остаются открыты.

17. [Семейство AIBehavior](native-ai-behavior-family-2026-09-10.md): 22 общих класса,
    21 PC factory/RTTI/default clone, 20 полных проверяемых lifetimes, 103 class
    operations. Pilot 0,896 с; пакет остальных 20 классов 9,956 с тремя процессами.
    PS2 независимо подтверждает все 21 размера/собственных конструктора/identity.
    24 пары lookup/selection за 9,389 с и 13 пар изменения блокировки за 1,928 с:
    unsigned keys, fallback key 0, повторные exit/enter при том же действии,
    сохранение/восстановление ключа, ненормализованный младший байт аргумента.
    Настоящие короткие action callbacks исполнены; тяжёлые callbacks не подменены.
    У IceGargoyle PC +1E0 / PS2 +1F0 не записано конструктором, но используется
    удалением; normal initialization остаётся открытой. Две ошибочные разведочные
    PS2 метки исправлены по vtable отдельными окнами, старые captures сохранены.
    Все 44 новых assessments previewed/applied/verified. Учёт 27,2674% / 18,4581%,
    502/733 и 436/681 оценённых классов. Полностью закрытых классов не добавлено.

18. [Семейство GenericTrigger](native-generic-trigger-family-2026-09-10.md):
    43 общих зарегистрированных класса, 41 paired factory identity. Все 41 PC
    factory/getter/clone/two teardowns прошли с объявленным окружением — 205 class
    operations; PS2 собственные constructors/size/identity проверены независимо.
    39 lifetimes потребовали только empty scene, StellaGlyph — существующую
    profile record с guard единственного byte2CB6, Chest — оригинальную привязку
    Node через419D00; оба Node освобождены. 21 пара cooldown/Notify/copy за3,083 с
    и14 пар Tick за1,953 с: границы интервала, active/one-shot/latch, порядок
    callbacks и реальные HUD/GameFlow queries. Геометрический predicate задан
    отдельно оригинальными constant leaves, callbacks не подменялись возвратом.
    У MarcusCersei подтверждено шестое различие registration/construction:
    запись GenericSideQuest, промежуточный constructor OutfitArtsSideQuest.
    База10/10→45/40, SideQuest впервые10/10, callable потомки20/15.
    Все86 assessments и113 sources проверены; учёт28,4475% /19,4200%,
    544/733 и478/681 оценённых классов. В манифест внесена проверка общей
    зависимости на пилоте до пакета; первичные неудачные captures сохранены.

19. [Node binding и MoveCtrl Copy](native-entity-bound-node-copy-2026-09-10.md):
    Barrel PC lifecycle с двумя original Node bindings прошёл за0,801 с; оба
    Node освобождены. MoveCtrl Clone всё ещё не проходит после привязки только
    источника. Оригинальный Copy между двумя заранее привязанными MoveCtrl,
    затем оба удаления, прошли за0,840 с. Поиск camera_lookat_head,
    model_root_master,hand_right использует Node назначения. Три PS2 scalar
    prefixes за0,117 с независимо подтвердили destination dependency и настоящий
    lookup consumer; полные Copy/Clone на PS2 не объявлены исполненными.
    Barrel PC15→20; MoveCtrl PC10→25/PS215→20. В первой подписи прежний PC score
    MoveCtrl ошибочно назван15; [отдельная поправка](native-entity-bound-node-score-correction-2026-09-10.md)
    исправляет актуальное описание, сохраняя старые imported SHA. Числа ledger
    всегда считались от фактического10; исправление не дало дополнительного credit.
    Три research rows/19 sources и одна description correction/14 sources
    проверены. Учёт28,4748% /19,4273%, 544/733 и478/681 классов; новых closed нет.

20. [Семейство GUIObject](native-gui-object-family-2026-09-10.md): шесть общих
    классов, шесть полных PC class lifetimes,30 операций; PS2 construction
    независимо проверена, у GUIObject/TextWidget factory встраивает часть ctor.
    18 пар Widget/Button selection6,264 с и27 пар Notify/setter dispatch3,842 с.
    Missing state использует normal Node, сохраняя index; pressed Node имеет
    приоритет над enabled; старый скрытый Node останавливает refresh.
    wxButton clone потребовал существующую strncpy fixture. PS2 run1 упёрся
    в MOVZ, отсутствующий в R4000; отдельный5KC integer allowlist, девять guards
    tests и run2 прошли. Старые источники/отказы сохранены, полного R5900/GUI
    исполнения не заявлено. Все12 assessments/36 evidence sources проверены.
    Учёт28,6999% /19,6256%,550/733 и484/681 классов; новых closed нет.

21. [Снаряды и их менеджеры](native-projectile-families-2026-09-10.md):22 общих
    класса,21 новый PC lifetime/105 class operations; прежний базовый manager
    reused. Все десять снарядов проходят после original AnimationManager factory:
    старый Clone405C22 требовал этот manager для создаваемого spActor. После
    удаления Actors registry пуст, manager удалён. PS2 независимо подтверждает
    все22 construction identities и ту же actor/controller dependency.
    35 manager пар9,201 с: pool4×5, registration scan только первых4, Tick
    strict unsigned expiry, pause, copy/Notify. Шесть пар Projectile copy и два
    полных PC Actor copy1,613 с: retained uint16 pointers и fresh Actor defaults,
    без переноса source Actor runtime settings. Ошибочная scout подпись Collision
    исправлена по RTTI на spActor; prefix boundary пропуск JAL/delay исправлен,
    старые runs сохранены. Все44 rows verified. Учёт29,3138% /20,1248%,
    570/733 и504/681 классов; новых closed нет.

22. [Сериализаторы: независимое дополнение](native-serializer-expansion-2026-09-10.md):
    20 новых PC lifetimes/100 class operations,29 PS2 factory/dual-vptr/getter
    identities;34 имени,13 прежних PC оценок сохранены. В PS2 28 inline factories
    и отдельный ctor PS2PartitionRenderable20DD70.84 unique secondary adjustors
    покрывают87 class-slot routes за1,474 с; только переход к payload body,
    а не его выполнение. Null Clone базы нельзя переносить на concrete классы:
    ошибочное ожидание pilot исправлено до исполнения Clone, код игры не менялся.
    Первичная PC65 CollisionInfoSerializer учитывает прежнее reader/index/writer
    и whole-load evidence как reviewed-existing, не новый реверс. Остальные19
    PC строк20,29 PS2 строк15. Все49 assessments verified. Учёт29,9209% /
    20,7636%,590/733 и533/681 классов. Восемь C++ spatial serializer Clone stubs
    теперь имеют достаточное native evidence для последующего восстановления.

23. [Clone восьми пространственных сериализаторов](native-spatial-serializer-clone-fix-2026-09-10.md):
    прежние явные null stubs заменены подтверждённым concrete factory/register/
    virtual Copy алгоритмом в общем Sparkplug. Отдельно сохранены16 PC/PS2
    native windows; прежние PC lifetimes reused. Новый тест до исправления
    воспроизводит ошибку, после —145/145, включая48 Clone assertions и97 прежних
    reader/ownership checks. Собрана только существующая проверочная цель.
    Дополнительные проценты за внедрение уже учтённого поведения не начисляются.
    [Отдельное уточнение размеров ProjectileManager](native-projectile-size-erratum-2026-09-10.md)
    добавляет пропущенную в тексте разность24 байта; исходные значения были верны.

24. [Четыре таймера](native-timer-family-2026-09-10.md): три новых полных PC
    lifetimes/15 операций, четыре независимые PS2 factory/vtable/getter identities.
    Прежний spTaskTimer PC83 сохранён.24 пары Start/Reset/Stop/Pause4,433 с:
    strict unsigned clamp, второй clock sample без повторного лимита; Game Reset
    сохраняет pause40.13 PS2 Task Update prefixes1,519 с, десять PC leaf сравнений:
    integer modulo время, source bit transfer, три PS2 child dispatch routes.
    PC multiply0.001 и PS2 float conversion/divide1000 записаны раздельно;
    R5900 FPU bit-exact исполнение отложено, новых правил игры не придумано.
    Все7 новых assessments verified. Учёт30,0982% /21,0059%,593/733 и537/681
    классов; новых closed нет.

25. [Проекции и FX](native-projection-family-2026-09-10.md):11 имён, три новых PC
    lifetimes/15 операций, четыре PS2 factory identities и восемь независимо
    проверенных объектных интерфейсов. У FX BaseObject расположен+4; initial
    PC pilot ошибочно читал primary render table, run2 использует original
    secondary RTTI/Clone/destructor. Два actual PC Setup/Clone/delete опыта:
    FX reused и удаляется, собственная геометрия Box/Pyramid остаётся factory
    defaults у clone. Прежний context56 остаётся: initial all-allocation assertion
    уточнён, глобальное владение не приписывается. Четыре PS2 Setup и шесть пар
    Init1,634 с,22 PS2 leaf/adjustor cases0,424 с. Разные PC/PS2 layer constants
    сохранены. PS2 TextureProjection58DA4026 опознаёт прежний PC dispatch literal,
    но PC RTTI класса нет; PS2 пока только metadata5. Две PC manager оценки
    сохранены. Все15 новых rows verified. Учёт30,3506% /21,2849%,599/733 и546/681.
    Добавлены общие ограниченные family-cache/getter-candidate инструменты.

26. [Сеть PC](native-network-family-2026-09-10.md): десять новых PC assessments,
    девять lifetimes/45 операций. Пять проходят с явным отказом WSAStartup и
    WSACleanup; успешная сеть не заявляется. Network/DXNetwork имеют object
    interface +4, initial RTTI ошибка обвязки исправлена по игре. Packet:
    17-byte header, fresh Clone, actual MemoryStream;15 случаев4,583 с,
    truncated/257-byte probes остановлены перед недоступным transfer. Все10
    rows verified. PS2 регистраций этих классов нет; оценки не перенесены.
    Учёт30,7053% /21,2849%,609/733 и546/681, новых closed нет.
    [Уточнение Projection context](native-projection-context-followup-2026-09-10.md):
    оставшийся56-byte объект независимо опознан как spDebugManager по getter/
    record/factory visits. Прежние sealed sources сохранены, новых баллов нет.

27. [Физические объекты и BV](native-physics-family-2026-09-10.md):13 имён,
    семь новых PC lifetimes/35 операций. Два исходных memmove boundary
    разрешены существующей CRT fixture. Три populated PhysicsManager ownership
    опыта подтверждают stable erase, оба registry и полное освобождение/global0.
    Два actual Capsule/Convex Clone сохраняют own factory geometry.12 PS2
    getter/null/prefix cases; combined probe1,617 с. GP mapping error исправлена,
    failed source/run сохранены. PS2 physical Named parent BoundingVolume
    доказан отдельно; registered parent остается BaseObject. Девять PC и четыре
    PS2 rows verified; PC BoundingVolume35 — первый учет прежнего исследования,
    не новая находка. Восемь прежних BV scores сохранены. Учёт31,0532% /21,4244%,
    618/733 и550/681; новых closed нет.

28. [Оставшиеся игровые классы](native-game-remainder-2026-09-10.md):50 имён,
    первый PC batch38 attempts/3 процесса16,576 с.28 полных lifetimes,
    четыре partial Clone и шесть factory boundaries;152 завершенные операции.
    Alpha после измеренного64KiB exhaustion проходит в128KiB; FairyStar после
    protected cap дошел до реальной null dependency, не объявлен passed.
    PS2:41 factory identities,46 getters и пять null Clone0,875 с.13 пар
    Vector/Perception/Movie data2,851 с; signaling NaN Y/Z — отдельная
    диагностическая неопределенность x87 стенда. Два новых physical ancestry
    исключения обеих платформ: MoviePlayer→BaseObject, wxParticleManager→
    spParticleSystemManager при иных registered parents. Все87 новых rows
    verified, три прежних сохранены. Теперь все игровые class names имеют
    оценки, но runtime classes не закрыты. Учёт32,1651% /22,4670%,661/733 и594/681.
    Десять PC lifetime зависимостей и NaN ограничение перечислены в досье.

29. [Общие классы движка](native-engine-core-remainder-2026-09-10.md):45 PC/
    39 PS2,21 PC attempts/3 процесса8,927 с;17 полных lifetimes и92 class ops.
    Actual AnimationManager разрешил LightController/MovieTextureController;
    embedded timer literal clock — CinematicManager, char_traits — Template.
    Четыре границы остались; LightEntity дошел до OutputDebugStringA после
    bounded original-vsprintf formatting.19 PS2 factory/table identities,
    39 getters. Восемь ConstEval cases1,686 с: PC translation и отдельные
    PS2 gates; Eval/no-op virtual slots переставлены. Специальная PS2 rotation
    FPU не эмулировалась. Все84 rows verified. AudioListener15 и три PC
    metadata-only5 — reviewed-existing, не новые методы. Учёт33,0382% /
    23,2012%,706/733 и633/681; новых closed нет.

## Ограничения

Публикация старого Viewer payload ранее отклонена automatic approval review,
ожидается отдельное согласие; новые push попытки не выполняются. Локальное
сохранение исследования разрешено. Предложения FAT/envelope и sort policy
сохраняются; разработка зависимых tools операций сейчас не ведётся.

Время остановки ещё не наступило.
30. [Платформенные классы движка](native-engine-platform-remainder-2026-09-10.md):
    27 PC и24 PS2 новых оценок. PC17 полных холодных lifetimes/91 операция,
    PS221 обычный constructor/table match,24 getter и2 отдельно доказанных
    secondary interfaces (+4). Исправлены три ошибочных primary dispatch
    исследовательского стенда, оригинальная игра не менялась. PCThread23
    случая решений по явным ответам Kernel32; потоки ОС не запускались.
    Mirror после protected-block factory и CRT memmove дошёл до общей
    зависимости Clone00412C01. Семь PC границ сохранены. Пакет24 попыток
    занял10,561 с/3 процесса; четыре адресных повторения2,969 с.
    51 row импортирована: PC33,6930%,733/733 оценены; PS223,7372%,657/681.
    Полный учёт каталога PC не означает полное понимание всех733 классов.
31. [Оставшиеся общие классы PS2](native-ps2-remainder-2026-09-10.md):
    24 новые PS2 оценки при сохранении всех прежних PC.23 original getters,
    16 обычных constructor/table matches и2 явно доказанных post-base prefix
    для Actor/TransformTrackEval.45 PS2 controller cases0,774 с и11 PC leaf
    counterparts1,481 с: Copybyte10, getters NodeController, RenderController
    finite-add, base Track hooks и18 intrusive-list операций AnimationManager.
    Список сохраняет links удалённого объекта; manager head/tail PS2+28/+2C
    против PC+24/+28. Случаи SQ/LQ и сложного FPU не подменялись generic MIPS.
    SceneGraphOptimizerPS2 пока metadata-only5.24 rows импортированы:
    PC33,6930%,733/733; PS224,3392%,681/681. Это полный первичный учёт
    зарегистрированных имён, а не завершение классов или всего EXE.
