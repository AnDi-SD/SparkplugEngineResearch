# PC SMO/SAN: цикл до 07:00 МСК 8 сентября 2026

Начат 7 сентября около 22:22 МСК после публикации накопленных изменений.
Срок завершения — 8 сентября, 07:00 МСК (`2026-09-08T04:00:00Z`).
Исходный commit цикла: `daa813859e013371c97377c544c4518d10014309`
(родитель CP99; включает последующие изменения SAN → VMD после публикации `8ac9451`).

Приоритет: общий loader/save, DX mesh materialization, Node resource graph.
Постоянные правила — [манифест исследования](../../docs/research/research-manifesto.md).
Стартовый PC workflow-v2: 45,193548%, 190 оценённых классов из 279;
PS2: 31,163265%. Семь PC критериев: 0 passed, 6 partial, 1 open.
Машинный план — [native-cycle-plan](../../research/native-cycle-plan-2026-09-08-0700.json),
полный локальный baseline — `local-data/results/cycle-20260908-0700/baseline.json`.

## Ресурсы и выбор первого участка

Ryzen 2700: 16 логических потоков; RAM 8 ГБ, на старте доступно около 2,6 ГиБ.
Рабочий бюджет исследования остаётся около 1 ГиБ суммарно. RTX 3070 доступна,
8192 МиБ VRAM, на старте занято 1605 МиБ. GPU выбирается для подходящих
массовых вычислений после замера; загрузка устройства сама по себе не результат.

Первый участок — полная запись FFPS/FAT. Проверенные writers отдельных
объектов и общий reference protocol используются повторно; сначала выясняется,
какая часть внешней orchestration действительно присутствует в PC binary.
На старте прежние capped whole-load сценарии не запускались повторно.

## Checkpoints

### CP99 — полный SAN producer и original reread

[Карточка результата](../../docs/research/native-pc-san-file-roundtrip.md).
В C++ добавлена ограниченная сборка целого FFPS с исходным index/reference
core. `bbush.san` воспроизведён побайтно и принят whole original `422B50`:
227 равных значений, 27 native assertions, 39 allocations освобождены.
Portable whole load/save/reload на четырёх SAN: 2800 равных значений и явно
проверенная native canonicalization capacity у `barrel` (20 → 21).
CTest 61/61. Score не повышен за композицию известных частей.

При подготовке проверки обнаружены и исправлены два различия между
ожиданием теста и фактическим контрактом: header validator запрещает offset
в конце нулевого stream; native writer добавляет field64 при его отсутствии.
Прежние capped native calls не запускались.

### CP100 — CPU/GPU пакетный поиск ссылок

[Проверенный инструмент и замеры](../../docs/research/native-pc-batch-xrefs.md):
434 цели, 12 546 одинаковых CPU/GPU кандидатов. CPU улучшен с 8,51 до 3,06 с;
GPU с подготовкой 1,88 с, сохранённый контекст — 16 мс на повторный запрос.
Пик working set около 198 МиБ. 5 CPU tests и 6 GPU fixtures прошли.
Это ускорение поиска, без повышения native score. Контекст освобождён после
эксперимента; для следующих пакетных запросов доступен резидентный режим.

Итоговые суммы цикла пока не объявлены.

### CP101 — план нового whole-file execution profile

После CP100 вводится отдельный профиль `file`: 1 млн инструкций / 8 секунд на
вызов, внешний процесс 30 секунд, heap в первом опыте 128 КиБ. Причина:
исторический `gameover.smo` останавливался точно на 100 000 инструкций при третьей
Node factory; два объекта укладывались примерно в 90 000. Лимит не доказывает
ошибку данных. Первым представителем выбран целый `bflower.san` через оригинальный
`422B50`, затем решение по фактическому результату. Старые micro probes остаются
отключёнными; сохранённый stopped guest не используется. Новых seams не
планируется. Перед опытом проверяются оба профиля и прежние ограничения памяти,
инструкций и доступа к ОС.

Результат: [6 whole-file сравнений прошли](../../docs/research/native-pc-whole-file-profile.md).
`bflower/barrel/bw` потребовали 112–134 тысячи инструкций; исходный C++ `barrel`
принят оригиналом. Целые `object.smo` / `gameover.smo`: 109 653 / 234 449 инструкций,
точные 2 / 6 Node records. Всего 221 native assertion, 3479 SAN значений,
8 Node records, все allocations освобождены. Предыдущий лимит действительно
мешал этой выборке; native поведения не подменялись. Проверяющий код исправлен
для float-типов JSON без изменения допуска. Class score пока не изменён.

### CP102 — startup, локализация следующих ограничений

Fresh manager factory `422F40` в file-профиле остановилась на 1 млн инструкций
в `888D5D`, успев выделить manager44, list24 и FAT88. Предел не повышается.
Fresh Animation initializer `6D1C10` прошёл native RTTI manager construction,
но запросил allocation `0xE000` (57 344 байта) через `470EF2`; старый предел
одного выделения 32 КиБ остановил опыт на 8319 инструкциях. Диагностический
запуск подтвердил конкретный размер и caller. Для отдельной новой startup-пробы
выбран предел одного allocation 64 КиБ при общей arena 128 КиБ; micro default
32 КиБ сохраняется. Это новый явно заданный профиль памяти, исходные stops
сохраняются локально. Проверка результата продолжается.

Новый startup-профиль принял запрос 57 344 байта, но затем остановился на
1 млн инструкций в `88C14E`; arena занята 57 440 байт. Полный startup остаётся
неподтверждённым. Увеличение execution budget не продолжается. Сохранены локальные
`startup-manager.json`, `startup-rtti-diagnostic.txt`, `startup-rtti.json`;
последний относится к 64-КиБ allocation profile. Это локализация ограничений,
без успешных native cases и без повышения score. Следующий участок — целый
SMO с мешами, используя уже проверенные hook/mesh/graph adapters.

### CP103 — whole `logo_screen.smo`

[Целый реальный SMO](../../docs/research/native-pc-whole-scene-file.md) прошёл
original `422B50` и C++: шесть объектов, пять основных рёбер, материал/pass/layer,
Fog, меш и declaration; 707 state/layer/buffer bytes плюс имена.
189 653 инструкции / 1,413 с; 71 536 байт из 128-КиБ arena, 50 native assertions,
81 allocation освобождена с отдельно обозначенным fixture cleanup combiner.
Исправлена схема тестового capture: PC texture states9 вместо общего host
массива12. CTest61/61, Python88/88. CP99–101 уже отправлены на GitHub;
CP102 сохранён локальным коммитом, следующий commit готовится.

### CP104 — shared DX payload и проверка аллокаций

[Оригинальные writer/reader/Init](../../docs/research/native-pc-dx-shared-payload.md)
сопоставлены с C++:110 точных байт, native cursor8 после полного копирования,
точные factory allocations28/20. Десять завершённых случаев,115 assertions,
22 allocations освобождены. Настоящий CreateIndexBuffer failure локализован
как null read в4C2AB6; guest остановлен и не возобновлялся. Отдельный seam
показал, что outer reader игнорирует false Init только при нормальном возврате.

В source устранена аллокация по непроверенным размерам заголовка; до выделения
проверяются сумма, остаток файла и32-МиБ предел. Добавлен отдельный reader с
native cursor для zero-origin contiguous stream. Общий runtime save/load graph
ещё не соединён: shared reader и inline full-consumption contract различаются.
CTest62/62. Native class scores не изменены.
