# PC SMO/SAN: цикл до 19:00 МСК, 7 сентября 2026

Начало: 07:48 МСК (04:48 UTC); крайний срок отчёта: 19:00 МСК (16:00 UTC).
Поручение: продолжить восстановление логики исполняемых файлов, приоритет
SMO/SAN и скорость исследования. Новые методы ускорения применяются в рамках
исследования без повторного согласования.

Предыдущий цикл завершён на CP50; новый начинается с CP51. Исходный снимок:
workflow-v2, 279 PC / 245 PS2; PC 44,10752688172043%, PS2 31,163265306122447%.
PC весь каталог 16,90450204638472%, движок 37,389057750759875%, игра
0,22277227722772278%. Исторические прямые 37 классов: PC 60,810810810810814%.
PC gates: 0 passed, 6 partial, 1 open. Сводка предыдущего цикла:
[0730](../../research/native-cycle-summary-2026-09-07-0730.json).

Оценки отражают подтверждённость поведения классов с равными весами, не долю
байтов EXE, готовность приложений или оставшееся время. PC и PS2 независимы.
Существующие незакоммиченные результаты предыдущих циклов сохраняются.

## Очередь

1. Связанный пробел CP50: RFX/parser/template и создание shader при cache miss.
2. Затем связная освещённая/текстурированная геометрия и оставшиеся независимые
   SMO/SAN read/save/lifetime ветви, по результатам исследования зависимостей.
3. Сборка, значимая регрессия и отчёт с конкретными ограничениями к сроку.

Сохраняются ограниченные guest-only native probes: 100000 инструкций / 2 с
на вызов, 30 с на дочерний процесс, арена 64 КиБ, запрос до 32 КиБ.
Оригиналы EXE/ресурсов используются только для чтения. Нет запуска игры,
перенаправления вызовов ОС/GPU, подставленного успеха неизвестным внутренним
методам или повторения ранее ограниченных вызовов без новых оснований.

## Результаты

Исследование начато. Подтверждённые checkpoints будут добавляться ниже.

### CP51 — parser: предобработка текста и lifetime

Восстановлен исходный `spParser` (размещение файла inferred): factory538,
таблица255 разделителей, `4D0BF0 -> 4D0AA0`, blank clone и освобождение.
Подтверждены удаление CR/LF, схлопывание tab/space, склейка слов после block
comments и отсутствие escape-семантики у backslash перед кавычкой. Найдены
точный размер native allocation и сохраняющийся старый end pointer; C++
сохраняет корректное владение и не воспроизводит выход за границы памяти.
Первый native RFX factory/lifetime также прошёл (728, parser base), но ещё не
выдаётся за готовое чтение RFX.

Проверено 11 точных сравнений native/source, 118 native assertions, 33 C++
assertions; профиль `pc-parser` — 12/12 дочерних процессов. Основной код
собран. [Карточка](../../docs/research/native-class-sp-parser.md).
Ускорение: извлечение исполненных адресов и декодированных инструкций из
существующего guest trace позволяет находить relocated тела protected
методов без повторной полной декомпиляции. Оригинальный EXE не изменяется.

### CP52 — шаблон эффекта и XML-проходы

Добавлены `spPCEffectTemplate` и `spPCRFXFileLoader` с подтверждённым TU.
Восстановлены constructor/document/identity/kind, отдельное владение именем,
диспетчеризация инициализации, создание и освобождение проходов, текст и
версии HLSL/assembly shader. Повторный Initialize добавляет проходы снова.
Проверены factory/blank clone RFX и отсутствие clone/factory у template.

В native-пробах исполняется весь исходный разбор небольших XML; в C++
внешний XML decoder передаёт уже декодированные события восстановленным
engine callbacks. Это явная граница библиотеки, не готовый полный XML parser.
Профиль `pc-effect-template`: 19/19, включая 18 точных сравнений; 171 native
assertions и 39 C++ assertions. Максимум на вызов — 71081 инструкция;
старые лимиты сохранены. [Карточка](../../docs/research/native-class-sp-pc-effect-template.md).
Полная сборка/CTest после CP51: 52/52. После CP52 новая цель собрана и прошла.

### CP53 — константы и сохранение данных между проходами RFX

Восстановлен producer44-байтовых shader descriptors: NAME/REGISTER, signed
decimal prefix, count1, пропуск отсутствующих атрибутов. Заполненные пробы
подтвердили сохранение списков параметров и active shader при reset прохода,
копирование этих списков в следующие проходы и освобождение. Добавлены
USAGE6 -> flag bit0 и точные ignored-node ветви; неизвестное исходное поле40
остаётся с маской известных битов, без придуманной инициализации.

`pc-rfx-events`: 9/9 точных сравнений,59 native assertions; C++ suite45.
Один случай проходит полный оригинальный XML parser с константой; восемь
проверяют все состояния после событий на подтверждённой границе XML library.
[Карточка](../../docs/research/native-pc-rfx-constants.md).
Ускорение: события проверяются по одному; пределы не увеличены. В диагностике
эмулятора отдельно выделено несовпадение calling convention после нормального
return: такое сообщение раньше ошибочно называлось instruction/time cap.

### CP54 — пять типов переменных RFX и populated ownership

Добавлены boolean/float/vector/color/texture producers, общий NAME/editability,
дубликаты имён, частичная инициализация диапазонов float и упаковка цвета с
усечением и wrap. Неинициализированные слова явно остаются неизвестными.
Native template destructor освобождает только общий string prefix переменной:
длинное имя файла texture остаётся в памяти. Подтверждена ровно эта утечка;
в C++ корректное владение строками её устраняет.

`pc-rfx-variables`: 8/8 точных сравнений,23 native assertions; все исходные
callbacks/constructors/vectors/destructors исполнены. Максимум13137 инструкций
и3184 байта арены. [Карточка](../../docs/research/native-pc-rfx-variables.md).

### CP55 — реальные Fixed.rfx и Bumpmap.rfx

Все76/106 engine XML callbacks оригинальных документов исполнены; полный
набор переменных, проходов, shader text и constants точно совпал с C++.
Декодирование XML — внешняя граница; original file/regex/XML wrapper целиком
здесь не объявляется пройденным. Fixed:5 переменных/1 проход; Bumpmap:7/1.
В Bumpmap подтверждены две уже установленные утечки texture filename tail;
других дельт владения нет. Source освобождает строки корректно.

`pc-rfx-corpus`:2/2,184 native assertions. Максимум23140 инструкций/вызов,
50896 байт арены. Длинные входные строки разрешены только по полному совпадению
с hash-verified ресурсом; копирование ограничено реальными известными областями
владения. Общие лимиты неизменны. [Карточка](../../docs/research/native-pc-rfx-corpus.md).

После CP55: полная сборка прошла, CTest53/53 (54,82с, parallel4,
timeout20s на тест). Workflow-v2 PC44,45%,190/279 assessed; gates0/7passed,
6partial. PS2 не менялся. Следующий участок: source specialization и compiler
output/reflection перед связыванием с shader-generating cache miss.

### CP56 — текст шейдера и результат компилятора

Полностью исполнена engine-функция4CFFE0 с явными выходами внешнего D3DX SDK.
Восстановлены header+declarations+code и вставка перед первым маркером с его
сохранением, выбор assembly/HLSL, reflection/parameter append, игнорируемый
HRESULT при наличии code buffer, копирование байткода и cleanup. Это не
реализация shader compiler или GPU. Добавлено владение code bytes в spDXShader.

`pc-shader-compile`:15/15 точных сравнений,90 native assertions; C++6.
Максимум13748 инструкций/вызов и2784 байта арены. Все engine allocations и
ожидаемые SDK objects освобождены. [Карточка](../../docs/research/native-pc-shader-compile.md).
Ошибки/повторная компиляция и полный manager generating miss остаются открыты.

### CP57 — полный generating miss менеджера

Исполнен весь4C8980: key -> строки -> shader factory ->4CFFE0 -> external SDK
-> descriptor append ->4CA030 -> external device -> cache insert -> return.
Повтор возвращает тот же объект без новой компиляции. Device HRESULT
игнорируется даже с NULL handle; shader object остаётся в кэше. Восстановлены
все поля source key,15 light entries и cap8 texcoord defines. Manager владеет
template40 и уничтожает его перед cached shaders. Добавлены аналогичные C++
ownership и SelectOrCreate; file loading пока остаётся явным prepared input.

`pc-shader-generation`:6/6 точных сравнений,53 native и24 source-harness
assertions. Максимум34158 инструкций/вызов,59072 байта арены.
[Карточка](../../docs/research/native-pc-shader-generation.md).
Следующий связанный consumer — автоматический draw с генерацией, затем
оставшиеся file/regex и error/lifetime ветви; SDK/GPU live не объявляется.

### CP58 — автоматический draw с первым созданием shader

Полностью пройден оригинальный4BC290 с generating miss, reflected MatDiffuse,
device create/bind/upload/indexed draw. Повторный вызов использует кэш и
обновляет изменённый alpha. Все device HRESULT игнорируются и в отрицательном
варианте. Source объединяет cached/generating automatic draw через общий core.

`pc-renderer-generated-draw`:2 точных сравнения,22 native assertions; C++12.
Максимум32256 инструкций/вызов и64992 bytes engine arena. Данные только внешних
SDK/device fixtures перенесены в их существующие interface pages; ограничения
не увеличены. Все engine allocations и SDK objects освобождены.
[Карточка](../../docs/research/native-pc-renderer-generated-draw.md).

После CP58: полная сборка и CTest55/55 (61,54с), cached-draw regression3/3.
Workflow-v2 PC44,56%,190/279 assessed; gates по-прежнему0/7,6partial.

### CP59 — полный file -> owned text helper

Оригинальный4D0A30 проходит через factory/пустой PC package manager, PC file
stream и memory stream. Подтверждены read-only Win32 параметры, неизменённые
bytes с embedded NUL/FF, рост capacity5000, destruction файла до добавления
uint32 zero и detach buffer. Пустой файл создаёт EOF diagnostic, но caller
игнорирует неуспех чтения и возвращает четыре нуля. Debug-output flag задан0;
реальные создание/форматирование/dispatch ошибки исполнены.

`pc-parser-file-text`:6/6 exact captures,36 native assertions; Parser C++51,
из них18 новых. Максимум27120 инструкций/вызов и21168 bytes arena.
[Карточка](../../docs/research/native-pc-parser-file-text.md).
Внешние seams только идентифицированные Win32/CRT; полный RFX regex caller
ещё открыт. Параллельно подтверждена успешная actual factory spPCPKManager;
она пока не равна восстановленному archive lookup.

### CP60 — настоящий RFX regex и metadata template

Весь4D56D0 выполнен без замены regex bodies: чтение файла, оба search,
capture1, hexadecimal scan, constructor и owned name. Подтверждены literal
case/order/spacing, newline wildcard, first matches, raw entities, empty
name и strlen truncation. Invalid-ID/missing-name дают точно одну утечку
file buffer5000; no-ID освобождает его. C++ сохраняет результаты и устраняет
эти утечки. Scanf EOF с unwritten ID пока только статическая граница.

Cold ID-regex constructor достиг instruction/time cap. Вместо возобновления
остановленной памяти найден отдельный законченный сценарий: в новой пробе
короткий original regex за81341 инструкцию готовит общую library; оба shipped
regex после этого создаются за56533/56000. Это доказательство warm library,
не закрытие cold startup. Все constructors/search/destructors реальные;
предоставлены только идентифицированные внешние MSVCP71/CRT/Win32 контракты.
После final regex dtor shared allocations и critical sections освобождены.

`pc-rfx-file`:14/14 exact captures,78 native assertions; C++50.
Максимум45075 инструкций/file call,81341/init,35104 bytes arena.
[Карточка](../../docs/research/native-pc-rfx-file.md).
Следующий связанный шаг — передать этот реально загруженный template в
исходный XML initialize и compiler-result consumer, затем вернуться к
оставшимся SMO/SAN graph/skin/load/save dependencies.

После CP60: обновление общей сборки выявило LNK1163 в старом incremental link
state. Debug build переведён на `/INCREMENTAL:NO`; все56 целей перелинкованы.
Повторная полноценная регрессия56/56 прошла за63,70с. Предшествующий CTest
со старой частичной сборкой55/56 не считается успешной верификацией.

### CP61 — file -> XML -> compiler на тех же объектах

Реально загруженный4D56D0 template передан в actual4D0650; оригинальная XML
библиотека создаёт pass, этот же shader record проходит4CFFE0. Проверены
assembly, HLSL/reflection и assembly с MatDiffuse из XML. Все строки,
параметры, ready transition, SDK request и owned bytecode совпали с C++;
все engine/library allocations и SDK objects освобождены.

`pc-rfx-pipeline`:3/3 exact,27 native и18 source-harness assertions.
Максимум72013 инструкций/рабочую фазу (81341 shared init),43376 bytes arena.
Source XML decoder — явная внешняя граница; native XML целиком исполнен.
[Карточка](../../docs/research/native-pc-rfx-pipeline.md).
Это три API calls на retained graph, не цельный manager/scene startup.

### CP62 — Skin factory и полный сериализатор с костями

Actual factory46A120 выделяет70h и задаёт weights4;490C50 выделяет14h.
Исправлен ошибочный source default0. Полные491170/490D30/490DA0 проходят
Model/Renderable, common resolver/FAT и исходный Node reader/writer.
Source получил настоящие Skin read/index/write adapters; переиспользуются
protected Model helpers, публичные Model class guards сохранены.

`pc-skin-serialization`:10/10 exact,144 native assertions; C++20 guards,
scene regression401/401 и EngineCore успешны. Максимум50962 инструкций,
6048 bytes arena. Все матрицы побитовы, повторные bone pointers canonical,
включая prebound; writer output целиком совпадает. Null bone error полностью
выполнен через original diagnostic object с внешними CRT contracts.
Повторное field/clear/null теряют ровно4+64 bytes; это нативная утечка,
стенд отдельно учитывает её до явной очистки. Source RAII её устраняет.
Skin не retain/release кости; source сохраняет явных shared owners.
[Карточка](../../docs/research/native-pc-skin-serialization.md).

### CP63 — Skin clone с настоящей общей таблицей

Весь412BE0→46A1A0→46A650→412C40 исполнен, включая native Node subtree
copy и412F70 mapping. Обнаружен и исправлен source fallback на исходную
кость: при miss оригинал создаёт Node. Root Clone сохраняет alias двух
одинаковых костей; direct Skin clone создаёт две разные, поскольку каждый
miss завершает отдельную root transaction. Ссылка на ребёнка уже
клонированной кости использует существующего child, без дублирования.

Source CloneManager получил map-aware shared-owner API; Node публикует
владельца созданного ребёнка. Устранён aliasing shared_ptr от исходной кости
к чужому mapped адресу. Хост требует реального владельца и отвергает
self-copy/cyclic unique-root edge; исходные UAF не воспроизводятся.
`pc-skin-clone`:8/8 exact,81 native assertions, максимум75460 инструкций,
1856 bytes arena, весь native heap очищен. Source тест дополнительно
уничтожает исходных владельцев и проверяет жизнь результата.
[Карточка](../../docs/research/native-pc-skin-clone.md).

После CP63 полная свежая сборка и CTest:57/57,57,77с. Отдельный
SkinSerializationTests теперь84/84, включая восемь source clone сценариев.

### CP64 — весь Skin render до device draw

Полный46A240 на подготовленном графе: actual Node affine, inverseBind*world,
identity world setter, mesh4BC670, material/pass4BC4A0, cached weighted
shader4BC290/4AE930 и indexed draw. Никакие engine bodies не подменены.
Source RenderUnlitForAnalysis соединяет те же уже перенесённые части;
ограничен fallback unlit material/NULL fog, declared device/renderer inputs.

`pc-skin-render`:5/5 exact,41 native assertions и70 C++. Pre false возвращает
false и сохраняет count9; SetTransform видит9, geometry/post видят1;
post false сохраняет1, полный успех сбрасывает0. Отрицательные device HRESULT
игнорируются. Skin weights0 не меняет этот consumer. Исправлен default
Model+28=FF000000, доходящий до rendererC194. Все device traces и палитра
сверены; max15288 инструкций,64576 bytes arena с поздним ResourceManager
teardown. [Карточка](../../docs/research/native-pc-skin-render.md).

После CP64 полная свежая сборка и CTest:58/58,57,91с.

### CP65 — прочитанный Skin до отрисовки без пересоздания графа

Actual FAT/Skin/Node read50702 инструкций → сохранённые Skin/Node/arrays →
полный46A240 и прежние shader constants/indexed draw. Source получает те же
directory/payload bytes, читает собственными FAT/Node/Skin readers и передаёт
полученный объект в render. `pc-skin-loaded-render`:3/3 exact,57 native
assertions. Normal/post-false/weights-zero; все device events и матрицы совпали.

Ускорение стенда: новый внешний reusable allocator в прежних64KiB. Между
завершёнными API calls настоящие clear methods освобождают entries read
infrastructure, actual destructors — serializers и transient PCErrorManager1024h.
Живые Skin/Node/arrays не двигаются, их байты
проверены после размещения renderer. Peak64944,26 range reuses,35 engine
generations — все освобождены ровно один раз. Старые probes сохраняют свой
allocator; лимиты инструкций/времени/request size не изменены.
[Карточка](../../docs/research/native-pc-skin-loaded-render.md).
Следующий фронт: SAN actor → сохранённая bone → palette; whole SMO resources
и full frame ещё не заявлены.

### CP66 — SAN actor меняет ту же кость до Skin draw

К прочитанному Skin/Node добавлены настоящий bbush.san object reader,
name registry/discovery/Start и manager dispatch в actor/evaluator. Explicit
world update421420(1), затем тот же46A240 до BlendMatrices и indexed draw.
Source использует настоящие readers и manager→actor dispatch. Четыре момента
0.25/0.5/0.75/1.25 дали4/4 **побитовых** capture,129 native assertions.
Сверяются sample, все local/world PRS, flags/count/frame и полный device trace.

SAN read39170, tick до4857, Skin read50702, render15288; peak64896 bytes,
75 owner generations полностью освобождены. Actor/animation уничтожены
между фазами, внешние engine/request inputs также возвращены allocator;
decoded Node удержан явным intrusive owner. Full simultaneous frame не
утверждается. [Карточка](../../docs/research/native-pc-skin-san-render.md).

Попутный статический факт для следующего фронта: global741654 (default40)
перезаписывается значением2 по5769C2 в init576820. Именно global читает ctor
5A3570 перед5A1600. Это возможная upstream связь с two-input ограничением,
но полный порядок игрового startup пока не доказан.

После CP66 повторный pc-skin-loaded-render3/3 и целевые CTest6/6 успешны.

### CP67 — actor configuration, queries и fade-stop

Factory читает741654; проверены capacities0/2/19/40 и whole5A1600 reset40→19/2.
В source добавлены явная default configuration и guarded replacement.
Статический caller51BD81 передаёт19 — найденная init-запись2 не является
универсальным пределом всех actor. Active reset исходно оставляет dangling
input pointers; host запрещает его, нативный unsafe путь не запускался.

5A14C0 выбирает первый pointer match (включаяNULL),5A1500 проверяет любой
match с counter>0 без running. 5A16D0 меняет только четыре поля первой
matching state: fade3/rate/threshold0/stopAfterFade1; без event/flush/Stop
до следующего tick. Duration>0 даёт inverse, иначе raw fallback, в том
числе отрицательный. Source API перенесены с объявленными finite/null guards.
`pc-actor-controls`:14/14 exact,171 native assertions; max8647 instructions,
5904-byte arena, all freed. [Карточка](../../docs/research/native-pc-actor-controls.md).

После CP67 обновлён весь static-link build:CTest58/58,63,81с. ActorBinding
default suite103/103, включая новые owner/reset/configuration guards.

### CP68 — capacity2 Start и control→Tick→Stop

Три real bbush resources, два named Node/controllers. После двух bound
Start третий whole5A1E30 возвращаетFFFFFFFF до request weight mutation,
binder/insert/event/flush; states/inputs неизменны. Restart существующей
animation разрешён и возвращает1. Никакого третьего input не исполнялось.

Actual FadeOutAndStop→manager/actor Tick→suppressed Stop: weight1→0.5→0
ещё running→negative-crossing Stop, очистка controller pointer pairs,
event9 и два flush в точном порядке. Отдельно sample0 и отрицательный rate
(weight1.125 без clamp). `pc-actor-control-pipeline`:5/5 exact,30 native
assertions; max4841/phase,15760-byte arena, all freed.
Source actions наблюдаются через прежний gated-manager/explicit Tick
контракт; настоящий source manager dispatch отдельно CP66.
[Карточка](../../docs/research/native-pc-actor-control-pipeline.md).

После CP68 прежние normal915/915 и fade_stop558/558 original/source
actor comparisons повторены успешно после изменения fixture capacity args.

### CP69 — весь Skin draw с первой генерацией shader

Сохранённый прочитанный Skin/Node →46A240→mesh/material→cache miss→4CFFE0
SDK→reflection4AF940→device4CA030→cache4C87A0→constants/draw. Source
SubmitUnlitGeometry/Skin context теперь передают optional generation inputs
в прежний DrawAutomatic. `pc-skin-generated-render`:2/2 exact,56 native
assertions,read50702/render42407 instructions,peak65488,52 owner generations
all freed. SDK bytes условные, не валидность реального shader/GPU.

Память ускорена без новых cap: best-fit reusable ranges, renderer caller
backing у верхнего края, COM tables в уже существующей interface page.
Уточнена ошибка атрибуции CP65:466760/4228A0 — clear entries, не FAT/manager
destructors. CP69 после actual clear снимает prepared global и освобождает
raw caller inputs; actual SkinSerializer/ErrorManager destructors прежние.
[Карточка](../../docs/research/native-pc-skin-generated-render.md).

После CP69 full static-link build и CTest58/58 (47,77с); прежний
pc-skin-render5/5 повторён успешно.

### CP70 — SAN → тот же Skin → generating draw

Четыре момента/режима real bbush SAN через actual manager/actor dispatch,
retained Node world cache и весь Skin shader-miss/SDK/reflection/device/
cache/constants/draw дали4/4 exact,173 native assertions. Пик65440 bytes,
92 owner generations all freed; read50702/39170,tick≤4857,render42407.
Source использует те же ранее перенесённые части; score не повышается
только за композицию. После fixture refactor прежние CP66 4/4 и CP69 2/2
успешны. World421420 пока explicit; SAN owners уничтожены перед renderer.
[Карточка](../../docs/research/native-pc-skin-san-generated-render.md).

### CP71 — source SceneManager world caller

Перенесён original67419388 borrowed list/current scene/blank clone/unconditional
singleton clear;6 exact captures,50 native и42 source assertions. Whole45A7D0
с actual Node, либо явными virtual callbacks: append виден в том же проходе,
remove-next пропускается,false-root не останавливает. Max1898 instructions,
1248-byte arena,all freed. Source SceneForAnalysis только view root14;
whole spScene ctor/subsystems/typed registrations не приписываются.
[Карточка](../../docs/research/native-pc-scene-manager-world-source.md).

### CP72 — scene manager world внутри SAN/Skin пути

Actual plain parent421A60 с translation(10,-20,30), затем только whole
45A7D0 вызывает421420 root→bone; test-side bone world call убран. Prepared
scene view/list explicit, scene membership отсутствует. Те же cached worlds
после original parent/manager teardown идут в palette/shader/constants/draw.
4 exact189 native assertions,world373 instructions,peak65440,98 owner
generations all freed. Full build иCTest59/59 (45,76с) успешны. Scores
сохранены. [Карточка](../../docs/research/native-pc-skin-scene-generated-render.md).

### CP73 — actual decoded mesh в Skin draw

42AFD0/429BC0 → real CPU buffers/4AA000/declaration4AE0E0 → тот же mesh
через479E20 в Skin first-generating draw. Flags803,stride28,84 vertex+6 index
bytes,triangle range и declaration array совпали.2 exact92 native assertions;
meshRead25186/render42407,peak65472,61 owner generations freed. COM payloads
в declared writable interface page; exact32+64 external stream prefix retired
после последнего read, без новых caps. CP64 5/5,CP69 2/2,CP72 4/4 regression
pass,sourceSkin70/70. [Карточка](../../docs/research/native-pc-skin-mesh-generated-render.md).

### CP74 — SAN/scene и actual mesh вместе

Соединены CP72/73 на тех же live Skin/Node/mesh:4 exact261 native assertions,
peak65424,107 freed owner generations. Whole mesh25186/render42407; SAN/
scene sampling и all raw metadata/PRS/device traces совпали. CP73 2/2 после
MeshRead refactor successful. Scores сохранены для композиции.
[Карточка](../../docs/research/native-pc-skin-scene-mesh-render.md).

### CP75 — decoded material внутри SAN/scene/mesh/Skin draw

Actual42F4C0/42F670 создают DXMaterial/pass/два StdLayer; тот же material
идёт в fallback renderer. Цвета/power/texture states доходят до device
и MatDiffuse constants.4 exact289 native assertions,materialRead14713,
mesh25186/render42408,peak65424,110 owner generations freed. CP74 regression
4/4 успешна. Skin20 пока NULL: собственная material-reference load ещё
открыта. Production алгоритмы прежние; оценки сохранены для композиции.
[Карточка](../../docs/research/native-pc-skin-material-mesh-render.md).

### CP76 — material owning reference внутри whole Skin read

Whole491170→47FBA0→4678B0 создаёт DXMaterial/pass/два Std и Node из двух
FAT entries; Skin20 держит один intrusive ref. Тот же материал выбран
423FD0 над отдельным белым fallback и освобождён Skin destructor. Actual
SAN/scene/mesh/cached draw:4 exact281 native assertions,read70998/render15298,
peak64656,103 owner generations freed. Source Skin допускает подтверждённую
ветку собственного unlit material; CP75 regression4/4 успешна. Specular3.5
даёт key01020011, что уточнило вход cached fixture. Ни caps, ни arena
не изменены. [Карточка](../../docs/research/native-pc-skin-owned-material-render.md).

После CP76 full build/CTest59/59 успешны (51,49с).

### CP77 — material override и сохранение renderer selection

Whole Skin graph подтверждает pre→save7400FC/clearC1C4→draw→restore→post.
Pre false не меняет bytes; post false уже восстановил byte, но сохраняет
active count1. C188 сохраняет прежний отдельный fallback/C194, при этом
material6C работает по Skin20.5 exact346 native assertions,render48..15305,
peak64656,103 owners freed. Source переносит общий save slot явно, без
stack guarantee. CP76 regression4/4 успешна; class score сохранён.
[Карточка](../../docs/research/native-pc-skin-material-protocol.md).

### CP78 — Fog codec → renderer/device state cache

Actual Fog43B910→4AD390→4B0A90,9 exact160 native assertions и123 source.
Identity cacheCA20 обновляется до type validation; invalid первый вызов0,
повтор1. Same pointer скрывает payload mutation до переключения. NULL
выбираетE4A0 fallback; rawNaN/negative-zero/ARGB и ignored HRESULT совпали.
Max164 instructions,62640-byte arena,all freed. Source ApplyFog перенесён;
добавлен60-й CTest target. [Карточка](../../docs/research/native-pc-renderer-fog.md).

После CP77/78 full build/CTest60/60 успешны (50,50с).

### CP79 — whole Skin Fog/material/bone references → draw

Actual491170 читает три refs из FAT, Fog43B910/423D20 сохраняет Skin24.
Те же Fog/material/Node проходят SAN/scene/mesh и весь46A240, Fog device
states идут до world transform. Invalid Fog setter0 игнорируется pre и
draw продолжается.9 exact675 native assertions,read74022/render≤15450,
peak64704,110 owners freed. Source Fog hook перенесён;CP77 regression5/5.
[Карточка](../../docs/research/native-pc-skin-fog-render.md).

### CP80 — тот же owning Fog graph с first shader generation

5 exact395 native assertions,read74022/render≤42569,peak65328,121 owners
freed. Skin20 material одновременно fallback layer input; actual generation/
reflection/device/cache/constants/handle lifecycle совпали. Fixture placement
alignment8/best-fit оставляет default16 прежним, не двигает live blocks и не
поднимает64KiB cap. Предварительные fragmented353-byte варианты с отдельным
fallback не продолжались; final uses same decoded fallback. CP75 regression
4/4 успешна. [Карточка](../../docs/research/native-pc-skin-fog-generated-render.md).

После CP79/80 full build/CTest60/60 успешны (55,30с).

### CP81 — decoded TextureData через material holder до whole Skin draw

Actual42C640/42C3B0/4ABBA0 читает native mip, actual41E870 владеет DXTexture,
whole46A240→4BBBA0→4BB650 связывает тот же COM handle.6 exact480 native
assertions:raw/DXT1/DXT3/DXT5/failed-device/post-false. TextureRead8703..8706,
render15592..15595,peak65128,113 owners freed и все COM refs/padding проверены.
Source whole submission теперь разрешает actual DXTexture и внешний handle;
native reader/holder ownership и shader cache прежние. Нативные bytes1C/24
сравниваются без соседней uninitialized padding. Caps не изменены.
[Карточка](../../docs/research/native-pc-skin-texture-render.md).

CP80 regression5/5 после CP81 успешна.

### CP82 — decoded texture и first shader generation

6 exact522 native assertions:raw/DXT1/3/5/failed-device/post-false; whole
Fog/material/bone/SAN/scene/mesh/texture graph до SDK/creation/cache/draw.
Render42711..42714,peak65416,130 owners freed; COM/SDK refs сверены.
Actual4AE140 после mesh read освобождает lookup tree без declaration/COM,
что убирает fragmentation353-byte request. Две прежние capped конфигурации
не продолжались. Source clear-map API и retained mesh declaration проверены.
[Карточка](../../docs/research/native-pc-skin-texture-generated-render.md).

CP81 regression6/6 после CP82, full build/CTest60/60 успешны (63,82с).

### CP83 — whole owning Skin alpha enqueue до callbacks

9 exact202 native assertions,124 default source;read70998/gate48..272,
peak54224,34 owners freed. Queue-disabled gate предшествует input reads,
capacity2048 следует sphere/metric; Skin всегда0 после enqueue. Renderer
flushing/alpha, object alpha, first blend и uint32 priority wrap сверены.
Native Skin46A230 mesh-null fault отличается от Model guard; новый fixture
использует actual DXMesh/setter. Source alpha queue carrier и Skin gate
перенесены;CP77 regression5/5 успешна. Full alpha flush/draw пока открыты.
[Карточка](../../docs/research/native-pc-skin-alpha-queue.md).

### CP84 — full alpha flush и actual RenderNode state publication

6 exact209 native assertions,237 default source;whole read70998,
flush26..487,peak65024,38 owners freed. Два actual RenderNode владеют одним
decoded Skin; queue A/A/B повторно не устанавливает A. Original4248D0/4BBB60
публикуют lights до COM и sphere после. False Skin pre игнорируется flush.
User callback append count3→4 посещён в том же проходе без повторного sort;
empty sort, prior-active reset и stale records сверены. External qsort input
явно уже ordered/distinct, CRT tie algorithm не заявлен. Source queue flush
и RenderNode consumer перенесены; actual geometry draw пока pre-cancelled.
[Карточка](../../docs/research/native-pc-skin-alpha-flush.md).

После CP83/84 full build/CTest60/60 успешны (48,25с).

### CP85 — queued full mesh draw и исправление PC bounds

10 exact257 native assertions:7 bounds35 и3 queued draw222. Расхождение
queue distance194/0 выявило отсутствие sphere/AABB producer в source DXMesh.
Восстановлены424230/468370/468000: all-vertex sphere, промежуточные float
stores и AABB по primitiveCount uint16 словам даже для32-bit индексов.
Отдельный общий analytical Mesh helper сохранён. Whole queued46A240→454850→
4248D0→46A240→4BE210 совпал normal/failed-device/post-false. Настоящий inner
return наблюдается read-only на45489E. Queue успех/count-clear не зависит
от отказа Skin post; Skin count остаётся1. Read70998,render≤15520,
peak64992,99 owners freed. Bounds1933..2373 instructions,all owners freed.
[Карточка](../../docs/research/native-pc-skin-queued-mesh-render.md).

CP82 regression6/6 и full build/CTest60/60 после CP85 успешны (47,93с).

Queued first-generation candidate после CP85 остаётся незавершённым:
476-byte template placement, затем158-byte native allocation при свободных
288 bytes fragmented72/96/8/48/64. Ни один capped frame не продолжался;
в exact/coverage не включён. [Граница](../../docs/research/native-pc-skin-queued-generated-boundary.md).

### CP86 — actual DXLight producer в whole lit Skin draw

16 exact1213 native assertions:all material modes0..7,light types0/1/2,
NULL/empty/disabled/ambient,failed-device/post-false. Whole read70998,
render15315..15713,peak64312,99 owners freed. Disabled/unlit ordinary
lights всё равно входят в shader key. Original4B53C0→4BDE50→COM→draw.
Source общий RenderForAnalysis/SubmitGeometry вызывает restored light core.
Обнаружены и исправлены mutable73FE98 вместо hardcoded black в modes1/6
и недостающий C194 мост Skin→material mode6 (80402010). CP32regression7/7,
CP85regression3/3;full build/CTest60/60 (54,11с) до finalC194bridge,
final profile проверяет мост и все16cases.
[Карточка](../../docs/research/native-pc-skin-lights-render.md).

### CP87 — light world → eight shader rows в whole Skin draw

10 exact796 native assertions;actual Node421420 на том же DXLight,
actual4B53C0/4AF940/45A6C0/4AE930, raw world/local и nonidentity view.
Ordinary/special constants, disabled и ambient groups, failed-device/post
совпали вместе со всей SAN/scene/mesh/material/draw цепочкой. Source теперь
берёт constants из тех же light objects, payload и выбранных groups.
Read70998,render≤16175,peak64488,102 owners freed. CP86 regression16/16;
full build/CTest60/60 (52,80с) включает finalC194bridge и CP87.
Class scores retained; cumulative264 exact8160 native assertions.
[Карточка](../../docs/research/native-pc-skin-light-constants.md).

### CP88 — complete Light/Data read/write и fresh-object round trip

19 exact184 native assertions плюс4 native-only38,source165/165.
Actual43FFD0/471590 allocation14;4400B0 игнорирует header и создаёт DXLight.
Whole440640/471670/440110/471AF0:9 fields,dirty8,ARGB conversion,unknown/repeat,
raw NaN/Inf/−0,mutable white73FE9C. False Enabled пропускается writer и
fresh DXLight reload включён; поведение сохранено. Native missing-terminator
success и partial-state error отдельно от строгих source envelope guards.
Read≤9633/write≤5862,876 engine bytes,all owners freed. Full build/CTest61/61
(52,19с). Cumulative283 exact8344 native assertions,дополнительно38
native-only assertions CP88. PC workflow45,01%;DataSer82,LightSer80,
Light86,DXLight79. [Карточка](../../docs/research/native-pc-light-serialization.md).

### CP89 — real SMO LightData и исправление quaternion writer

19 exact92 native assertions:9 pristine SMO sections72 +10 matrix cases20.
В corpus_id2 выбраны9 compact field shapes (512rows,исполнены только9).
Working corpus исключён по SHA до guest execution; original files intact.
Whole4400B0/440640/440110/fresh read,30words Node+Light state. Bloom_body
выявил native w3F3504F4 против source3F3504F3:464CB0 сохраняет trace/root/
reciprocal на x87stack. Shared FromMatrix widened,обе branches/ties сверены.
Read≤13220/write≤7772,876enginebytes,allownersfreed. Node regression11/11,
Skin-light-constants10/10;full build/CTest61/61 (45,43с). Cumulative302
exact8436 native assertions плюс38 native-only CP88.
[Карточка](../../docs/research/native-pc-light-corpus.md).

### CP90 — decoded Light через full virtual world producer

6 exact86 native assertions,по5 последовательных world operations.
4400B0/440640 уже производит payload внутри Node section до light fields;
4B58D0 capturesflags→428C30/421420→post-baseEnabled→4B53C0. Source overrides
соединены; dirty8/1,stale raw setter,disabled clear,inherited8,parent world
(17,−12,39) совпали. Read11411/world≤513,424/676enginebytes,all ownersfreed,
включая late SceneManager от parent attach. Light Scene3CNULL явно.
Corpus9/9 и Skinconstants10/10regression;full build/CTest61/61 (43,60с).
Cumulative308 exact8522 native assertions плюс38native-only CP88.
[Карточка](../../docs/research/native-pc-light-world.md).

### CP91 — unchanged SMO LightData через complete lit Skin draw

11 exact921 native assertions:9 pristine compact SMO lights и2 failure
variants case6. Whole header/read→virtual world→same Light→8 shader rows→
SAN/scene/mesh/own-material/Skin/draw. ReadSkin70998,LightRead2398..5109,
LightWorld92..184,render≤16175,peak64624,105 owners freed. Empty Node ambient
выявил отсутствовавший source constructor dirty8:428D5B OR B0,8 восстановлен;
initial decoded payload теперь совпадает. CP88 regression19/19 после fix.
Cumulative319 exact9443 native assertions плюс38native-only CP88.
[Карточка](../../docs/research/native-pc-skin-decoded-light.md).

Full build/CTest61/61 после final CP91 constructor fix успешны (62,15с).
First lit generation candidate не закрыт: после новых размещений и более
раннего чтения Light остаётся fragment353 при64928 reserved. Нет повторов
неизменённого capped call и нет exact/coverage credit.
[Граница](../../docs/research/native-pc-skin-decoded-light-generated-boundary.md).

### CP92 — decoded Light, actual Scene attachment и world target refresh

6 exact186 native assertions;actual Scene/root/4managers/2RenderNodes и
decodedDXLight вместе. Attach421A60→Scene3C/list;world4B58D0→428C30→46ACE0→
46A850/cache→DXpayload. Девять операций type0/1/2/3,disabled/shadow,range,
hierarchy100,sphere/inherited8. Position-only1 обновляет GPU payload,но cache
пересчитывается только после8: source сохранил различие. Новый borrowed
manager/target bridge не выдаётся за полный sourceScene/autoAttach.
World≤2720,arena54304,engine1989,allownersfreed. CP91regression11/11.
Cumulative325 exact9629 native assertions плюс38native-only CP88.
[Карточка](../../docs/research/native-pc-light-scene-world.md).

Full build/CTest61/61 после CP92 успешны (47,83с).

### CP93 — actual selected RenderNode cache через whole Skin draw

11 exact954 native assertions:9 unchanged SMO lights +2 failure modes.
Actual manager46AB80/45A780→LightWorld→46ACE0/490B50 fills actual RenderNodeF0;
тот же cacheC190 идёт в wholeSkin/shader/constants/draw. Source cache resolver
использует те же spDXLight/sourceObject; slots не подставляются. Selection
≤2363,render≤16175,peak65072,108ownersfreed. Borrowed scene view ended before
draw;actual Scene attach отдельноCP92. Cumulative336 exact10583 native
assertions плюс38native-only CP88.
[Карточка](../../docs/research/native-pc-skin-selected-light.md).

Full build/CTest61/61 после CP93 успешны (52,88с).

### CP94 — real Light graph index/write/repeat/null/fresh load

9 exact135 native assertions. Real SMO Light owns actual Node child;whole
4672C0/4639D0 indexes2objects;467350/440110/463F10 writes graph andaliases.
RuntimeDXLight6B3E7BAA persists in header/FAT вместо исходногоLightData5E6402DF.
Prepared saved-entry directory→actual466B90/4678B0/factories/read/attach loads
freshgraph;repeatcanonical,null4bytes,allownersfreed. Index14169,write≤14560,
read≤71333,arena6864,wire77..115bytes. Source201/201;opaqueDC excluded.
FAT directory producer/fullFFPSsave остаются открыты. Cumulative345 exact10718
native assertions плюс38native-only CP88.
[Карточка](../../docs/research/native-pc-light-graph-roundtrip.md).

### CP95 — whole PC MeshData writer

30 exact210 native assertions:10 CPU mesh inputs × policies0/1/2. Actual
41A270/434E40 deep copies/bounds→429EA0→013BC5E0 temporary DXMeshData→полные
CPU buffer writers. Fixed u32 field lengths, bit20 expansion только в planning
header, packed bytes в payload. Source writer и source backend reread проходят;
SparkplugMeshReaderTests217/217. Native write≈19–22k instructions,arena≈1KiB,
allownedfreed. DXMeshDataSerializer66→78,DXMeshData55→62;workflow45.14%,190/279.
Cumulative375 exact10928 native assertions плюс38native-only CP88.
[Карточка](../../docs/research/native-pc-mesh-writer.md).

### CP96 — source mesh export → whole original reader

10 exact290 native assertions:5 formats×policy0/1. Source output сначала
совпадает с original writer, затем свежий original429BC0 читает именно эти
байты черезCPU→DX/declaration/COM. Весь VB/IB и5metadatawords совпали сsource
reader. Arena≤63824,read≈30–31k;allownersfreed. File-byte boundary и8byteheader
fixture явные;неwholeFFPSsave. Full build/CTest61/61,53.58s. Cumulative385
exact11218 native assertions плюс38native-only CP88. Scoresretained.
[Карточка](../../docs/research/native-pc-mesh-writer-roundtrip.md).

Closeout accounting tests:61/61. Исправлена устаревшая привязка проверки
PC/PS2 очередей к конкретному первому work-item; теперь проверяется полный
platform-specific состав, а порядок зависимостей покрывает отдельный тест.
Scopeaudit:81 recorded class/platform pairs,missing0,invalid0.

### CP97 — whole общий MeshData writer

30 exact210 native assertions. Actual42AEF0/42B170:policy0/2→field0u32length,
42B030index/vertexwriters,terminator;policy1→единственный00безbuffershelper.
Source virtual writer/context реализован,295/295Mesh tests. Allownersfreed;
writer1772/≈9800instructions,неwholeFFPSexport. Cumulative415 exact11428 native
assertions плюс38native-only CP88.
[Карточка](../../docs/research/native-pc-mesh-base-writer.md).

Финальная арифметическая сверка по каждому checkpoint исправила прежние
cumulative суммы: CP51–61 дают861 native assertions, а не961. Поэтому итог
CP51–97 —415 exact comparisons /11428 native assertions плюс38native-only.
Локальные результаты checkpoint, число сценариев и оценки классов не менялись;
исторические импортированные манифесты сохранены без изменений.

Final review CP97: exact-type host guard исключает наследование base PC
writer неподтверждённым PS2 serializer, в том числе policy1 omission.
Три дополнительных source assertions проверяют отказ до output mutation;
MeshReaderTests теперь298/298. Это guard, не новое native доказательство.

### CP98 — mesh writers подключены к generic reference protocol

4 exact48 native assertions:base/DX×policy0/1 packedmesh. Whole4672C0→5A7DB0
индексирует один ресурс без CPU-buffer relationships;467350→actualwriter
пишетCPUclassheader,полныйpayload,FATmetadata,repeatID/0иnull. Initialprobe
обнаружил неочищенныеFATcontainers;actual466760доteardownзакрываетвсехowners.
Source IndexRelationships подключён,346/346Mesh tests. Final cumulative419
exact11476 native assertions плюс38native-only CP88. Scoresretained.
[Карточка](../../docs/research/native-pc-mesh-graph-writer.md).

Финальная полная сборка/CTest после CP98:61/61,47.95s. Accounting61/61,
scopeaudit81pairs/missing0/invalid0. Последние8profiles103casespassed;
99повторныхи4новыхCP98. Итог419exact11476native+38native-only.
48cyclemanifests неизменны;94evidencepaths существуют;текущиеhashesдляклассов
цикла совпадают. Три исторические dossier-ссылки содержат прежние хеши
дополненных позже карточек;immutable manifests не переписывались.

Cycle complete: 2026-09-07T16:00:24Z. Final report: docs/research/native-cycle-report-2026-09-07-1900.md.
