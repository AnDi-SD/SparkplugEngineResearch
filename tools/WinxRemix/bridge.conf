# Winx Club: install beside NvRemixBridge.exe as .trex/bridge.conf.
# 96 MB reproduced an UnlockRect transport failure while loading Alfea01_02.
# 192 MB is a tested workaround; the internal corruption is not fully diagnosed.
# See tools/WinxRemix/docs/guide.md.
clientChannelMemSize = 192MB
